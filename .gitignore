# OS / エディタ
.DS_Store
Thumbs.db
.idea/
.vscode/

# 環境変数・秘密情報
.env
.env.*
!.env.example

# 言語別(必要に応じて調整)
node_modules/
dist/
build/
__pycache__/
*.pyc
.venv/
target/

# Rust/wasm ビルド成果物
engine/pkg/

# T022: Edaxの配布バイナリ(公式リリースからdownload-edax.ps1で都度取得する。
# ライセンス上の再配布可否が未確認のためコミットしない)
bench/edax-compare/edax-extract/
bench/edax-compare/*.zip

# T040: WTHOR棋譜データ(FFO公式サイト https://www.ffothello.org/informatique/la-base-wthor/
# から都度ダウンロードして学習にローカルで使うのみ。再配布可否が明記されていない
# グレーゾーンのデータのためコミットしない)
train/data/

# T080: Codex CLI (scripts/codex-task.*) の実行ログ出力先
logs/

# T080: eval_cli等のデバッグ実行で作業ディレクトリ直下に生成されるログ・出力
err_*.log
out_*.json

# T080: bench/edax-compare でローカルビルドするeval_cli系バイナリ
bench/edax-compare/*.exe

# T098: resumable per-position raw endgame benchmark checkpoints
bench/edax-compare/endgame-results/

# T192: Logistello book (https://skatgame.net/mburo/log.html) の生データ
# (logbook.wtb.gz/.wtb)と、そこから抽出した全件JSON。GPLだが再配布可否が
# 明記されていないため生データはコミットせず、都度download-logbook.ps1で
# 取得し直す。全件抽出JSONも生データ由来の巨大生成物のためコミットしない
# (コミット対象はサンプリング済みcuratedセットのみ)。
bench/logistello/data/
# T192: verify_wld.pyのresumable per-position完全読みチェックポイント
bench/logistello/verify-results/
.claude/launch.json
