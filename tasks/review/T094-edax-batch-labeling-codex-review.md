# T094 最終レビューレポート

## (a) 重大（done を止めるブロッカー）

### 1. 新旧等価性が成立しておらず、検証条件も旧実装を再現していない

新しい教師バッチAPIは、[vs_edax.py](C:/Users/yoshi/work/othello-trainer/bench/edax-compare/vs_edax.py:87) および [vs_edax.py](C:/Users/yoshi/work/othello-trainer/bench/edax-compare/vs_edax.py:565) で Edaxを常に `-n 1` で実行します。一方、変更前の `edax_solve()` は `-n` を指定せず、Edax既定のマルチタスク設定を使用していました。

作業ログの等価性検証は「旧1子1プロセス」側にも `-n 1` を指定しており、`4386455` の旧実装そのものとの比較ではありません。このため、受け入れ基準の「新旧実装で depth/score/bestValue/diffFromBest が全件一致」を満たした証拠になっていません。

実際に同梱Edaxと `problem/small-30.obf` を用いて、同一30局面をlevel 16で以下の条件により直接比較しました。

- 旧実装相当: `-l 16`、`-n` 指定なし
- 新教師API相当: `-l 16 -n 1`

最終探索行のscoreを比較すると、problem #15で旧条件 `-11`、新条件 `-09` となり、不一致を再現しました。したがって、これは単なるプロセス起動方法の変更ではなく、教師値を変え得る探索設定変更です。

同じ理由で、作業ログの8シャード性能A/Bも両側を `-n 1` に揃えた測定であり、旧本番実装から新実装へ移行した場合の性能改善を示していません。`+21.34%` はバッチ化単体の参考値にはなりますが、受け入れ基準で求められた旧→新の実効比較としては不十分です。

対応には、次のいずれかが必要です。

- 旧実装と同じEdaxタスク設定を維持して等価性・8シャード性能を再検証する。
- `-n 1` による教師値変更を仕様変更として明示的に裁定し、等価性要件を更新したうえで、旧本番条件との性能・品質差を改めて測定する。

現仕様ではexact/level配分などの品質変更はスコープ外であり、実装判断だけで教師値の意味を変更することは認められません。

## (b) 中（次タスクで対応すべき）

なし。上記ブロッカーの解消が先です。

## (c) 軽微（記録のみ）

- [gen_teacher_corpus.py](C:/Users/yoshi/work/othello-trainer/bench/edax-compare/gen_teacher_corpus.py:729) のコメントは、シャードなし設定では既存smoke checkpointとrun keyが完全一致すると説明しています。しかし、今回追加された `edaxTasksPerProcess` と `elapsedMsPolicy` により旧checkpointとは必ず不一致になります。現在の挙動に合わせて説明を更新すべきです。
- レビュー環境は書き込み可能な一時ディレクトリがないため、pytestは再実行できませんでした。これはコード不具合としての指摘ではありません。作業ログには `10 passed` が記録されており、パーサの連番・件数検証、非ゼロ終了コード許容、level別バッチ、子順序、resumeのテスト追加自体は差分上確認できました。
- `git diff --check 4386455..5220fe3` は問題なし、レビュー終了時の `git status --short` はクリーンでした。変更は指定されたPython 3ファイルのみで、engine・train・`compare_pattern_v3.py` に差分はありません。

## (d) 総合判定

**不合格**

バッチ分割、`problem # N` の連番・件数検証、exact深度確認、子順序維持、平均化した `elapsedMs` の由来記録、openings SHA-256のrun key追加など、実装構造は概ねタスク仕様に沿っています。

しかし、`-n 1` の追加によって旧実装と教師scoreが変わることを実機で再現しており、新旧等価性と旧→新性能比較という主要な受け入れ基準が成立していません。教師コーパスの値そのものに関わるため、doneを止めるブロッカーです。