# T156d 最終レビューレポート

対象コミット: `c95a6e3924f179e19fd396b5658d9c85e2afb9a8`

## (a) 重大（doneを止めるブロッカー）

なし。

redo #2で指摘された到達不能コードは解消されている。4件のcanonical SHA-256検証は `validate_inputs()` の先頭に配置され、Gate 2/3集計より前に必ず実行される。

## (b) 中（次タスクで対応すべき）

なし。

## (c) 軽微（記録のみ）

なし。

## 確認内容

- 差分は以下の2ファイル、40行追加・4行削除に限定されており、スコープ外変更はない。
  - `bench/edax-compare/compare_mpc.py`
  - `bench/edax-compare/test_compare_mpc.py`
- Gate 2 corpus、oracle positions、oracle labels、v4 weightsの4件すべてについて、SHA-256検証が到達可能になった。
- 現在の4入力のSHA-256はコード内の期待値と一致している。
- `main()` は `validate_inputs()` が成功した後にのみ `gate2()`、`gate3()`およびreport/meta生成へ進むため、改変入力による集計をfail-closedで防止できる。
- 回帰テストは各canonical入力を個別にコピーし、先頭1バイトを反転して、対応するSHA-256エラーになることを4つのsubtestで検証している。他入力はcanonicalのままなので、各検査を独立に確認できている。
- 書き込み禁止のレビュー環境では `TemporaryDirectory` を作成できず、新規テストの4 subtestは環境要因で実行不能だった。一方、残る9テストは通過し、既存の別ファイルを各引数へ差し替える読み取り専用確認では4入力すべてが期待するSHA検査で拒否された。
- `git diff --check c95a6e3~1..c95a6e3` は問題なし。
- レビュー中のファイル変更はなく、`git status --short`も空だった。

## (d) 総合判定

**合格**

redo #2で唯一残っていたブロッカーであるcanonical SHA-256検証の到達不能問題は、正しい検証位置への移動と4入力すべての改変拒否回帰テストによって解消されている。変更は局所的で、集計ロジックや既存測定値を変えるものではなく、回帰リスクも低い。

Gate 3自体の不合格は実測結果として許容されるものであり、既存レポートどおりMPCをdefault OFFのままとしてT156eへ進まない判断が維持されるため、本タスクの完了を妨げない。