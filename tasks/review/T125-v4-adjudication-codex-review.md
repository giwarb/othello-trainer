# T125 最終レビューレポート

## (a) 重大（done を止めるブロッカー）

1. `tasks/` 配下のファイルが成果物コミットに含まれています。

   コミット `913f183` には、本来の成果物 [t125-report.md](C:/Users/yoshi/work/othello-trainer/bench/edax-compare/endgame-results/t125-report.md) に加え、[T125-v4-adjudication.md](C:/Users/yoshi/work/othello-trainer/tasks/T125-v4-adjudication.md:50) の作業ログ13行が含まれています。

   これは以下に反します。

   - `AGENTS.md`: `tasks/` は作業ログ追記のみでコミットしない
   - `AGENTS.md`: `tasks/` を変更対象にしない
   - 受け入れ基準: 「変更対象ファイルのみパス指定でコミット」

   さらに、コミット済みの作業ログ末尾には「コミットハッシュは未作成」「タスクファイルはコミット対象外」と記載されており、実際の状態と矛盾しています。

   `913f183` から `tasks/T125-v4-adjudication.md` の変更を除外し、成果物コミットをレポートだけに修正する必要があります。作業ログと実際のコミットハッシュの管理は、オーケストレーター側のタスク管理手順に従ってください。

## (b) 中（次タスクで対応すべき）

なし。

## (c) 軽微（記録のみ）

なし。

## 検証結果

測定内容そのものには、採用判断を覆す誤りは認められませんでした。

- `git log ed22fd2..913f183` は1コミット。
- `git diff ed22fd2..913f183` はレポート追加とタスク作業ログ追加のみで、コード変更なし。
- 追加seed 4/5/6の学習結果、epochs 20、frozen MAEを生データと照合。
- 6seed regretを再集計し、`0.7000 / 1.6667 / 0.9667 / 1.0333 / 0.8333 / 1.4333`、平均`1.1056`、標本SD`0.3702`、中央値`1.0000`を確認。
- 全6 oracle JSONでv2 regret `1.5666666666666667`、60局面を確認。
- 候補seed 3のSHA-256 `c372b83366c4006023ae05f3af5b68dda5929aca7ff7308d1b398a89639e383f`を実ファイルから確認。
- T125結果を再集計し、60局、4勝2分54敗、平均`-24.0167`、中央値`-24`を確認。
- T108は正式なTT64結果、T121はv3結果を使用していることを確認。
- 30 openingすべての先後平均と、v4-v3・v4-v2の対応差がレポート表と一致。
- fixed-depth 40局面、node-budget 10局面とも2回一致を確認。
- run key、重みSHA、openings SHA、Edax SHA、ハーネスSHAが生resultsと一致。
- `git diff --check ed22fd2..913f183` はPASS。
- `git status --short --untracked-files=all` は空。
- コード変更がないため、`cargo test -p train` / `cargo test -p engine`をT125で省略した判断は妥当。

「v3維持・素のv4採用見送り」という推奨は、oracle改善に対して実戦点推定がv3比`-2.7833`石、v2比`-2.1667`石で、さらにgzip約`+3.36MB`という結果から妥当です。CIが0を跨ぐことや選抜バイアスの限界も適切に記載されています。

## (d) 総合判定

**不合格**

測定結果、比較、統計的留保、採用推奨はいずれも仕様に沿っており、内容面は合格相当です。しかし、`tasks/` ファイルを成果物コミットへ含めたことが明示的なリポジトリ規律および受け入れ基準に違反するため、現状のコミット範囲ではdoneにできません。`tasks/T125-v4-adjudication.md` を成果物コミットから除外すれば、再レビューでは合格可能な状態です。