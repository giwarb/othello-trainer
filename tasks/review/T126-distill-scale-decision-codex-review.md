# T126 最終レビューレポート

## (a) 重大（done を止めるブロッカー）

なし。

主実験は規定の3 seedで完走しており、生のoracle結果も確認した。全6実験で60局面が揃い、M2ガードのv2 regret=1.5666666667を完全再現している。候補regret、CI、重みSHA-256もメタデータと一致した。結論の根幹である「WTHOR@18万=3.8222 ≥ 2.5」は十分確認でき、量が支配的という判定は仕様に沿っている。

## (b) 中（次タスクで対応すべき）

### 1. 実験provenanceが、実際に使用した実装を特定できていない

[t126_distill_scale_decision.meta.json](C:/Users/yoshi/work/othello-trainer/bench/edax-compare/t126_distill_scale_decision.meta.json:5) は以下を記録している。

- `gitCommit`: `71a160f...`
- `gitTree`: `8b5dd9...`（71a160fのtree）

しかし、主実験で使用した `--train-subset-size` は71a160fには存在せず、今回の953b15bで初めて追加されている。そのため、記録されたコミットをcheckoutしてメタデータ記載の学習コマンドを実行しても再現できない。生oracle JSONも同じ基準treeを記録していた。

今回はコマンド、重みハッシュ、生結果、現在コミットされた実装を相互照合でき、数値や結論が誤っている証拠はないためブロッカーとはしない。ただし今後は、未コミット実装で実験する場合、少なくとも実行時のトレーナーソースSHA-256またはdirty diffハッシュをprovenanceに含めるべきである。

### 2. 正の小さいサブセット指定が、空の学習集合として正常終了する

[train_patterns_v3.rs](C:/Users/yoshi/work/othello-trainer/train/src/bin/train_patterns_v3.rs:258) は `--train-subset-size=0` のみ拒否する。一方、層ごとに `floor(target * phase_count / total)` を取るため、小さい正のtargetでは全層の採用数が0となり得る。

その場合、[サブセット適用箇所](C:/Users/yoshi/work/othello-trainer/train/src/bin/train_patterns_v3.rs:290) は空配列を受け入れ、学習はno-opのままゼロ初期重みを正常な成果物として保存する。T126の180,110件では発生しないため実験結果への影響はないが、新設CLIの正しさとしては次回修正対象である。抽出後に空集合を拒否する検証とテストを追加すべきである。

## (c) 軽微（記録のみ）

### 1. 「T109と同じ方式」という説明は厳密には一致しない

T109側はシャッフル前にフェーズ内レコードを安定キーでソートしており、元ファイル順が変わっても同じ集合から同じサブセットを得られる。一方、今回の実装はWTHOR読み込み順のindexを直接シャッフルしている。

WTHORファイル一覧はソート済みで、現在のパーサーも決定的なので本実験の再現性には問題ないが、「T109と同じ」ではなく「T109と同じ層化・prefix方式」と表現する方が正確である。

### 2. 100万件の「likely reaches v2」はやや強い表現

[投資比較表](C:/Users/yoshi/work/othello-trainer/bench/edax-compare/t126_distill_scale_decision_report.md:87) は100万件を「likely reaches v2」としているが、計画レンジ1.4–1.63はv2の1.5667をまたいでおり、統計的CIでもない。本文では外挿の不確実性を適切に説明しているため判断への重大な影響はないが、「v2付近に到達する可能性」に弱める方が整合的である。

## (d) 総合判定

**合格**

理由は以下のとおり。

- v4×WTHOR@18万を3 seedで測定し、平均regret 3.8222を確定している。
- 全oracle実行でM2ガードを完全再現している。
- 蒸留18万、WTHOR18万、WTHOR全量の比較と、仕様の2.5石閾値に基づく帰属判断がある。
- 副実験として45k/90k/180kの入れ子学習曲線と、100万・400万への慎重な外挿がある。
- 100万、400万、見送りの比較表と段階投資の推奨が提示されている。
- スコープ外のコーパス生成、本番配線、v4アルゴリズム変更は行われていない。
- 差分はコード1ファイルと指定成果物2ファイルに限定され、`git diff --check`および現在の`git status --short`は問題なし。
- 生の重み・oracle結果とメタデータの数値／ハッシュは一致した。

provenanceと小サイズ入力の問題は改善すべきだが、今回の実験結果や投資判断を覆すものではなく、doneを止める必要はない。