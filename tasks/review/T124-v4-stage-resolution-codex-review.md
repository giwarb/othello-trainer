# T124 最終レビューレポート

対象範囲: `025eac7..f3d4466`  
対象コミット: `f3d4466` 1件  
総合判定: **合格**

## (a) 重大（doneを止めるブロッカー）

なし。

v4の61段・1石刻み定義は、PWV3のシリアライズ／ロード、engine評価、WTHOR SGD、蒸留特徴抽出の各経路で一貫して使用されています。従来のPWV1/PWV2および13段PWV3は13段・5石刻みに固定されており、明白な互換性回帰は認められませんでした。

ローカルに残る実験成果も読み取り確認し、以下がコミット済みメタ情報と一致しました。

- WTHOR・蒸留各3seedの重みサイズとSHA-256
- 全6 oracle結果の各60局面完走
- v2 regretの全回 `1.5666666667` 再現
- WTHOR平均 `1.1111111111`、seed SD `0.4992587`
- 蒸留平均 `2.8666666667`
- rawおよびgzipサイズ

`git status --short --untracked-files=all`も空でした。

## (b) 中（次タスクで対応すべき）

### 1. コミットされた実験メタ情報の来歴が不足している

`t124_v4_stage_resolution.meta.json:9-41`にはコマンド、結果、重みSHA-256はありますが、次の識別情報がありません。

- WTHOR入力データのハッシュ
- 蒸留200kコーパスのSHA-256
- oracleコーパスのSHA-256
- `eval_cli`、Edax本体、Edax評価ファイルのSHA-256
- 評価時のgit commit/tree
- bootstrap seed・反復数
- NPSで使用した28局面集合の識別情報

生のoracle JSONにはこれらの一部がありますが、`train/data/t124`はgitignore領域なので、将来そのローカル成果が失われるとコミット済みメタだけでは実験条件を厳密に特定できません。直前のT123メタにはコーパス、git tree、評価バイナリ等のハッシュが記録されており、T124でも同程度の来歴を残すべきです。

数値自体は今回ローカル成果から照合できているためブロッカーとはしませんが、次の実験メタ作成時に共通スキーマ化することを推奨します。

### 2. 仕様上の主比較であるv3 `1.40石`がレポートの主結論になっていない

タスク仕様は「主比較: v3×WTHOR（1.40石）」ですが、レポート冒頭 `t124_v4_stage_resolution_report.md:5-7` はT111の3seed平均 `1.4778` を基準に「0.3667石改善」と結論づけています。

T121採用候補の `1.4000` は表の `:39` とメタの `:21` に記載されているため、受け入れ基準上の比較自体は存在します。ただし主比較として明記すべき差は、

- v4平均 `1.1111`
- v3採用候補 `1.4000`
- 改善量 `0.2889石`

です。3seed平均同士の `0.3667石` は補助比較として併記する構成が仕様に忠実です。採用判断を変える差ではありませんが、改善幅をやや大きく見せる表現になっています。

## (c) 軽微（記録のみ）

### 1. PWV3説明の一部がv2固定のまま

`train/weights/README.md:138-141`ではv4の61段対応を正しく説明していますが、同ファイルの予測値説明 `:145-151` は以下の点でv3/v4を反映していません。

- 「22パターンインスタンス」と固定しているが、v3/v4は38
- `stage_for_empty_count(...)`と記載しているが、v4では重み自身のステージ定義を使用
- 重み参照の説明がv1/v2までで、PWV3の自己記述パターンを含まない

また `:127-130` のPWV3ヘッダ説明も13段を一般形式のように記載し、その後のv4段落で例外を補っています。機能への影響はありませんが、PWV3共通仕様とv2/v3・v4の具体例を分けると誤解がありません。

## (d) 総合判定

**合格**です。

理由は以下のとおりです。

- v4はv3と同じ38 instance／10 classを維持し、ステージ解像度だけを61段／1石刻みに変更している
- engineのロード・評価と、WTHOR／蒸留両学習経路がモデル固有のステージ定義を参照している
- 既存v2/v3のステージ定義とバイナリ互換性が維持されている
- ステージ境界、PWV3 round-trip、評価結果の基本テストが追加されている
- WTHOR・蒸留とも3seedの結果が揃い、M2ガードも全回通過している
- サイズ、gzip配信量、NPS、ロード時間、working setが報告されている
- 平滑化を追加しなかった判断と、本番配線・採用裁定を行わないスコープが明確である
- checkpoint/resume実測が記録され、リポジトリ状態もクリーンである

中指摘は実験来歴とレポート表現の改善事項であり、今回の実装・測定結果の正しさを否定するものではないため、doneを止めるブロッカーとは判定しません。