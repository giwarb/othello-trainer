# T120 最終レビューレポート

レビュー範囲: `a9ac46f..a4cc5d5`

変更は実験メタデータとレポートの2ファイルのみで、コード変更はありません。実データからoracle regret、paired bootstrap CI、T113回帰式・prediction interval、各SHA-256を独立再集計し、記載値との一致を確認しました。`git diff --check`はPASS、`git status --short --untracked-files=all`も空です。

## (a) 重大（doneを止めるブロッカー）

なし。

3seedすべての学習成果物、60局面oracle評価、各回のv2 regret完全再現、checkpoint、結果JSONが存在し、コミットされたメタデータおよびレポートと整合しています。

## (b) 中（次タスクで対応すべき）

なし。

T090cへ進む推奨も「v2との同等性が立証された」とはせず、点推定の悪化と広い信頼区間を明記した限定的な判断になっており、結果から逸脱していません。

## (c) 軽微（記録のみ）

- レポートの「in-corpusだけの見かけ上の改善ではない」という表現は、統計的に確認された事実よりやや強めです。T113 fullの2.8石から2.3889石への差について直接のpaired検定は行われておらず、確認できるのは独立oracle上でも点推定が改善したことです。ただし、後段ではoracleの広い不確実性を適切に説明しているため、主要結論への影響はありません。
- 3seed SDの0.0694石は学習seed間のばらつきであり、60局面oracleの標本不確実性ではありません。レポートではSDと局面bootstrap CIを分けて記載しているため誤りではありませんが、次タスクで候補間比較に利用する際も両者を混同しないことが重要です。

## 検証結果

- コーパス: 200,000行、SHA-256はメタ記載値と一致。
- 3seedの最終重みSHA-256: すべてメタ記載値と一致し、各`final.bin`は`best.bin`と一致。
- oracle regret: `2.4667 / 2.3667 / 2.3333`、平均`2.3889`を生JSONから再現。
- v2 regret: 全3ファイルで`1.5666666666666667`を再現。
- seed別paired bootstrap CIおよび3seed平均差`+0.8222`、CI `[-0.2333, 2.0000]`を再現。
- T113回帰: 200k予測`1.9196`、180,110件予測`1.9778`、4点回帰のprediction interval `[1.0610, 2.7782]`を再現。
- 5点再fit: 傾き`-1.0113`、R²=`0.9515`を再現。
- `gitCommit`と`gitTree`は実験時の基準コミット`a9ac46f`およびそのtreeと一致。
- 閾値20、exact率低下、T113とのコーパス条件差について適切な留保があります。
- T110 M1'の呼び出し順は既存コード上で`ensure_metrics_header`から`truncate_metrics_after`となっており、対応済みです。
- コード変更がないため、本差分固有の回帰リスクは認められません。

## (d) 総合判定

**合格**

受け入れ基準の主要項目はすべて満たされています。実験結果と統計値は保存された生データから再現でき、外挿未達、v2との差が有意でないこと、T090cへ進む価値を限定的とすることの3点も結果に即して慎重に記述されています。軽微な表現上の留保はありますが、結論や採否候補の判断を覆す問題ではありません。