# T108 最終レビューレポート

## (a) 重大（doneを止めるブロッカー）

なし。

指定差分は計測ハーネス2ファイルとレポートに限定され、スコープ外の`engine/`・`app/`変更はない。主要な計測値はローカルcheckpointとも一致した。

## (b) 中（次タスクで対応すべき）

1. 160k速度集計の`completedPositions`が誤解を招く

[endgame_bench.py:609](C:/Users/yoshi/work/othello-trainer/bench/edax-compare/endgame_bench.py:609)では、探索が完走した局面数ではなく、時間比を算出できた局面数を`completedPositions`として返している。

今回の160k系列は全102 runが未完走にもかかわらず、機械集計上は`completedPositions: 34`となる。コミットされたレポートは正しく「0/102 run」と明記しているためT108の結論には影響しないが、後続処理がこのフィールドだけを読むと「34局面完走」と誤認する。

`measuredPositions`と`fullyCompletedPositions`を分離するか、`completedPositions`を全反復完走局面のみ数えるべきである。

## (c) 軽微（記録のみ）

1. `play_game()`の説明が新しい無制限経路と矛盾している

[vs_edax.py:695](C:/Users/yoshi/work/othello-trainer/bench/edax-compare/vs_edax.py:695)にはwall-time系列では必ず`--time-ms`を付ける旨が残っているが、直後に追加されたT116経路は意図的に`time_ms=None`、`max_nodes=None`で実行する。実装は正しいため、コメントのみ更新が望ましい。

2. 新しいT116切替に対する単体テスト追加はない

ただし正式60局のテレメトリで、空き21以上890手がbudgeted設定、空き20以下556手が無制限設定となり、後者556/556がexact完走したことを確認できる。今回の受け入れを妨げるものではない。

## 確認結果

- C2無制限系列:
  - 34局面、102/102 run完走
  - 幾何平均19.165672倍
  - 中央値17.785699倍
  - p90 47.570238倍
- C2 160k参考系列:
  - 幾何平均0.042896倍
  - 中央値0.039906倍
  - p90 0.151323倍
  - 完走0/102である旨がレポートに明記されている
- FFO #40～49は全問完走し、正解値・ノード数・壁時計がcheckpointと一致
- 対Edax level10:
  - 4勝54敗2分
  - 平均石差-21.85、中央値-23
  - run keyにquota 60%、T116閾値20、TT 64MiB、openings SHA-256を収録
- appの本番方針と対局ハーネスの切替値は一致
- checkpointはC2 540件、C3 48件、速度各204件、対局60局を保持
- `git diff --check f6c4910..2d92de9`は問題なし
- `git status --short`は空で、タスク由来の未コミット差分はない

## (d) 総合判定

**合格**。

T108の目的は計測結果の確定であり、シリーズ性能ゲートの達成自体は完了条件ではない。要求された二系統のC2壁時計比、E50参考値、FFO、60局対局、決定性、checkpoint/resume、provenanceおよび過去比較上の注意が揃い、主要数値も生データと整合している。

なお、シリーズ性能ゲートそのものは、無制限経路の幾何平均19.166倍が完了線5倍およびストレッチ2倍を超えているため、レポート記載どおり不合格である。