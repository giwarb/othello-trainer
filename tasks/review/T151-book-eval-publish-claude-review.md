# T151 最終コードレビュー(Claude代替レビュー、対象 af144b3、T150のbuildDb重み拡張含む)

- 総合判定: **合格**(重大なし、中1件・軽微7件)。ただし中M1のデータを受けオーケストレーターが仕様更新を裁定(下記)。

## 正しさの確認(最重要観点)

- 符号・パス・終局の3分岐(openingBookPositions.ts)は vs_edax.py の analyze_game_losses_v2/terminal_value と規約完全一致。テストは実データ全10,745 requestsをothello.tsオラクルとの独立再計算で全数検証。実証スポットチェック(root f5=0、虎ライン0〜+1、f5-f6-e6-f4のg7がloss20で除外、f5-f6のd3/c4がloss9で除外)も符号規約の正しさを裏付け。
- フィルタ×assignWeights: 全460ノードで重み合計1(誤差0件)を実測確認。到達不能刈りBFSはキー規約含め正しくテスト済み。
- app配線: josekiDb state 1箇所切替でCPU着手・トレース・cap・josekiHitの4消費箇所が連動。練習系不変。トレース命名の全エッジケーステスト済み。n_tasks=1決定性担保。

## 中

**M1. 命名済み定石カバレッジの大幅縮小(コードバグではなく設計の帰結、データで規模が判明)**
- 1111→460(58%減)は浅い層の除外エッジが連鎖的にサブツリーを孤立させる構造的必然(除外135中26本がply≤4)。
- 既存joseki.json 615ノード中、新ブックに残ったのは**240ノードのみ**。命名済み112ライン中**全手順生存は43ライン**、69ラインが途中切断(32ラインは5手目以内: 虎D/虎E=4手目loss3、猫/羊=5手目、野兎=3手目loss9)。
- 除外135件中**76件がloss=2ちょうど**(うち64件が命名済み経由)= level16評価の±1石の揺れで採否が反転する境界。
- 帰結: ユーザーが実在の定石を打つと即「(離脱)」・cap解除。従来(フェーズ1)より定石体験が浅くなる。
- **オーケストレーター裁定(2026-07-20)**: 命名済みライン由来の手は除外対象外(警告記載のみ)とし、ロス≥2除外は自動抽出手のみに適用する仕様へ更新。T151続行で再ビルド。

## 軽微

1. 全bookMoves除外後の非葉ノード7件が残存し、isLeaf=falseのため悪手判定ソースが「定石」表示になる軽微不整合(capは付かず実害小)。
2. eval_opening_book.pyのcheckpointが設定ドリフト(level/バイナリ変更)を検知しない(定数固定スクリプトのため実害低)。保存はバッチ単位(200局面≒数秒)で長時間ルールの趣旨は充足。
3. 全滅終局の石差が空マス加算なしの生石差(T084と同一の既知規約、序盤ブックで実害なし)。
4. AUTO_NAME_REがtraceDisplay.tsとbuildOpeningBook.tsで二重定義(DRY違反)。
5. opening-book.jsonのlines[]は刈られたライン込み363件素通し(現状消費者なし、将来の消費者が欠損に遭遇しうる)。
6. fetch失敗時はブック無しで継続(graceful、joseki.jsonフォールバックなし)。
7. パス含みラインのキー規約破綻は既存制約を意図的に踏襲(現データにパスなし、コメント明示)。
