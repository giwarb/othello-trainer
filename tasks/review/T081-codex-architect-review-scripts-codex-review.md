レビュー結果は **不合格（T081 は done 不可）** です。

### (a) 良い点

- `-s read-only --ephemeral -o` が指定されています。
- 入力ファイル、出力の非空、終了コードの確認があります。
- 対象コミットは意図した4スクリプトのみです。
- ログ・レポートの出力先は概ね仕様どおりです。
- `git diff --check` に問題はありません。

### (b) 問題・不足

- `99dcf7a` の PowerShell版はプロンプトをargvで渡しており、引用符を含むT081で壊れる問題が残っています。
- stdin渡しとPowerShell 5.1向けUTF-8対応は現在の作業ツリーにはありますが、`99dcf7a` には含まれていません。
- コミット版のshもプロンプトをargvで渡しています。
- shファイルはGit上で`100644`のため、Unixで`./scripts/codex-review.sh`のように直接実行できません。
- Range検証は通常の不正値を拒否しますが、`--help`など先頭が`-`の値への防御が不十分です。
- 現在4スクリプトに未コミット変更があり、作業ログの「scripts差分なし」と食い違っています。

### (c) 必要な改善

- 現在のstdin・UTF-8修正を修正コミットとして確定する。
- T081自身を入力にして、PowerShell 5.1で次を再検証する。

```powershell
./scripts/codex-review.ps1 tasks/T081-codex-architect-review-scripts.md `
  -Range "99dcf7a~1..99dcf7a"
```

- 終了コード0、出力の日本語非文字化け、プロンプトがstdin経由であることを確認する。
- shに実行権限を付け、macOS/Linuxでも検証する。
- Rangeの先頭`-`を拒否するなど、revision検証を強化する。

### (d) 最大のリスク

「検証済み」と記録された修正がレビュー対象コミットに入っていません。作業ツリー上で直っていても、`99dcf7a`および既にpushされた成果物には反映されません。

したがって、未コミット修正を確定して再検証するまで、T081の状態は `review → redo` が妥当です。今回は読み取り専用環境のため、`tasks/STATUS.md`とタスクファイルの状態更新は行っていません。