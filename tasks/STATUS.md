# プロジェクト進捗ボード

最終更新: 2026-07-14 (オーケストレーター、セッション再開。T084の verifier+codex-review を並列起動し状態を review に変更)

## 【次セッションへの引き継ぎ 2026-07-14 13:00】T084はベンチ完走・検証待ち。次はverifier+codex-review→T085a委譲(→再開済み、検証並列実行中)

**T085b起票時の必須申し送り**: T084 codex-review のブロッカー1(resume判定をエンジン/ハーネス/重み/Edax/eval_cli.exeバイナリのidentityまで含めて厳格化、不一致なら既存チェックポイント拒否+eval_cli再ビルド)と中所見2(チェックポイントのアトミック書き込み=一時ファイル+置換方式)を、**ベンチ実行前の前提修正として T085b のスコープ先頭に入れる**こと。要件10(フォールバック回帰テスト)はT085aに追記済み。

**現在地**: T084 redo#1 の実装(Codex gpt-5.6-sol、コミット ad88c91)とクリーン版120局ベンチ+修正版弱点分析(コミット 1346f18)まで完了。**残る手順**: (1) verifier に受け入れ基準検証を委譲(redoフィードバック7項目+追加要件6(--max-nodes)の確認)、(2) codex-review(範囲 `ee6bea3..1346f18` 目安)、(3) 両方合格で T084 を done、(4) **T085a(起票済み: `tasks/T085a-exact-node-budget.md`)を `./scripts/codex-task.ps1 tasks/T085a-exact-node-budget.md -Model gpt-5.6-sol` で委譲**。設計書は `tasks/design/T085-beat-level10-report.md`(§1がT085aの規範)。
**確定ベンチ結果(クリーン版)**: single-root L10 0勝20敗(-35.8)/L5 4勝16敗(-21.6)/L1 14勝6敗(+16.5)、allmoves L10 -49.0/L5 -33.6/L1 +16.4。ロス362件・負値0(オラクル修正成功)。フェーズ別平均ロス: 中盤+2.36/序盤+1.88/終盤+1.82。テレメトリ最重要所見: **空き20〜24で平均到達深さ3.0・タイムアウト100%**(=T085aの修正対象)。
**注意**: 弱点分析途中でPC停止したためではなく完走済み。T084作業ログの最終追記はimplementerが停止したため未記入の可能性(verifier委譲時に成果物ベースで判定させる)。

## サブエージェント構成の再編(2026-07-14 ユーザー指示)

ユーザー指摘「Sonnet implementerには難しかったのかも。gpt-5.6-solに難しい実装もやらせたい。役割も多すぎ、タスク完了まで時間がかかりすぎる」を受けて再編。変更内容:
- **実装の難易度ルーティング新設**(CLAUDE.md): エンジン/アルゴリズム/横断変更/redo1回以上/設計レポート起点 → Codex gpt-5.6-sol(`codex-task.ps1 -Model gpt-5.6-sol`)。それ以外 → implementer(Sonnet)。
- **tester廃止**(テストは実装ワーカーが書く運用が既に定着)、**reviewer廃止**(最終レビューはcodex-reviewに一本化、redo後再確認はverifier)。パイプラインは「実装1体 → verifier+codex-review並列 → 判定」に簡素化。
- **codex-task.ps1をT081方式に近代化**(stdin渡し・UTF-8・unelevated sandbox・UTF-8 BOM)。旧版は引数渡しバグ+旧AGENTS.mdの委譲指示でCodexが混乱する構造だった。
- **AGENTS.mdを全面改訂**: 旧「Codexをオーケストレーター扱いする」テンプレ(古い・プロジェクト情報なし)から、「Codexは常にワーカー」前提のガイド(プロジェクト概要・ビルド/テストコマンド・ワーカー規律・長時間実行ルール)に書き換え。codex-design/review のプロンプトの AGENTS.md 無効化文言も新前提に更新。
- オーケストレーターの直接編集は例外規定(ユーザー直接指示)を適用(.claude/agents/・AGENTS.md・scripts/codex-*.ps1)。スクリプトは構文検証済み、実運用初回(T084 redo想定)で動作確認する。

## 【次セッションへの引き継ぎ 2026-07-14 09:55】ユーザー指示により一時停止。T084がredo待ち

**現在地**: AI強化(Edax接近)ロードマップ(T083設計レポート: T084計測→T085 exact/時間管理→T086 TT→T087/T088評価・学習→T089探索→T090蒸留)のT084を実行中に、ユーザー指示で一時停止。

**T084の状態 = redo(attempts 1)、再委譲はまだしていない**:
- 実装コミット `ba1b834` は機能的にはほぼ完成(cargo test 139件・FFO不変・`eval_cli best`テレメトリ・fixed-depth決定性・ロス全件>=0・チェックポイント/resume(対局側)は合格済み)。
- しかし**致命バグ**: exact試行が時間切れでdepth1も完走しないと`best_move=None`→ハーネスが終局と誤解釈し、single-root対局の80%が空き18〜19で途中終了。**「level10勝率0%→40%」等の対戦結果は汚染データで意思決定使用禁止**。
- 修正指示の全文はタスクファイルの「フィードバック redo #1」に記載済み(エンジン側合法手フォールバック+ハーネス側エラー化+loss resume粒度+provenance+120局再実行)。
- **再開手順**: implementerに `tasks/T084-bench-single-root-telemetry.md` を渡してredo実行(フィードバック欄参照と伝える)。完了後はverifier+codex-review(併走可)で再検証 → done後にT085(exact切替・時間管理+**ノード数予算方式**(ユーザー方針2026-07-14、下記メモ参照))の起票へ。

**確定済みのユーザー方針(T085に反映すること)**: 探索制限は壁時計ではなくノード数予算ベース(決定論的)を主とし、壁時計は普段発動しない保険のみ。深さベース単独は不採用。

**残置プロセスの注意**: 昨日からのアイドルpythonプロセス(PID 3752)が残っていれば無害だが停止してよい。

---

## 【過去の引き継ぎ 2026-07-13 12:10】T082: 対戦60局完走・弱点分析のみ残り(完了済み・記録として保持)

**状況(12:05時点)**: T082(Edax自動対戦ハーネス)は実装完了・スモークテスト合格済み。**本対戦フル実行の対局フェーズ(レベル10/5/1×各20局=60局)は完走し、全局チェックポイント保存・コミット済み(cbf3095)**。担当implementerエージェントはユーザーが手動停止済み。バックグラウンドのpython実行(PID 20996)だけが生き残って弱点分析(負け対局のEdax level 16再評価)を実行中だが、誰も監視していないため、完走しても`vs_edax_report.md`の保全・コミットは次のオーケストレーター確認時に行う(PCシャットダウンで消えた場合は弱点分析のみ再実行すればよい)。

**対戦結果(3回目実行、engine=depth10/exact18/time1000ms vs Edax book off)**:
| Edaxレベル | 勝敗 | 平均石差(engine視点) |
|---|---|---|
| level 1 | **16勝4敗** | +15.3 |
| level 5 | 0勝20敗 | -29.1 |
| level 10(同深度) | 0勝20敗 | -37.2 |
(参考: 1回目実行では level 5 = 2勝18敗、level 1 = 12勝8敗。傾向は一致)

**保全済みの成果物(implementer停止のためオーケストレーターが例外的に代行コミット)**:
- `bench/edax-compare/vs_edax.py` — ハーネス本体(完成。3つの実バグを踏んで修正済み: (1)eval_cliの`--time-ms`なし時の組合せ爆発→`--engine-time-ms`既定1000msで対策、(2)終局済み局面をEdaxに渡すとパース失敗→終局判定を先に行う修正、(3)Edax本体が瞬時解決局面で0除算クラッシュ(exit 0xC0000094)→終了コード非ゼロでもstdoutをパースする修正。詳細はT082作業ログ)
- `bench/edax-compare/vs_edax_results.json` — 60局全部の棋譜・石差・自エンジン評価値(`loss_analysis`はnull=未了)
- `tasks/T082-vs-edax-match-harness.md` — 詳細な作業ログ

**次セッションの再開手順**:
1. まず`bench/edax-compare/vs_edax_report.md`と`vs_edax_results.json`の`loss_analysis`の有無を確認する(生き残りのバックグラウンド実行が完走していれば両方揃っている→そのままコミット保全してverifier検証へ進む)。無ければ新しいimplementerにT082の残作業を再委譲する。指示: 「タスクファイルと作業ログを読むこと。対局60局は完了済み(`vs_edax_results.json`にコミット済み)。**まず vs_edax.py をCLAUDE.md『長時間実行タスクの運用ルール』(2026-07-13追加)に準拠させる改修を行う**(チェックポイント粒度を1局/1分析単位に細分化、起動時に既存チェックポイントを読み込み完了済み分をスキップして再開するresume機能=保存済み60局を再対戦せずに弱点分析から再開できること、進捗の逐次ログ出力)。その上で弱点分析+レポート生成を完走させ、コミット・pushまで完了させよ。所要約10〜20分」。改修要求の経緯: 一括書き出し設計のせいで1回目実行の60局が消失、レベル単位チェックポイントでも中断時にレベル5の途中経過が消失した(ユーザー指摘 2026-07-13)。
2. 完了後: verifier検証 → レポート(`vs_edax_report.md`)を読み、codex-design(`./scripts/codex-design.ps1`)へ「エンジン強化方針」の設計諮問(依頼書に explorer調査のギャップ一覧(下記)+対戦実測データを含める)→ 強化タスク群に分解。

**暫定的にわかっていること(1回目フル実行の結果より)**: 自作エンジン(depth10/exact18/time1000ms)はEdax level 1 にやっと勝ち越し(12勝8敗)、level 5 に2勝18敗、level 10 に0勝20敗。Edax level 10 とは同深度なので、**差は主に評価関数の精度**とみられる(explorer調査の仮説と整合)。弱点分析(フェーズ別ロス)の完成が待たれる。

## タスク一覧(ユーザー追加依頼 2026-07-13: AIをEdaxに近づける強化)

ユーザー依頼: 「AIをもっと賢くしEdaxになるべく近づけたい。まずEdaxと戦わせつつ弱点の原因を探り、強くする工夫を考えて実装する」。explorer 2体の事前調査(2026-07-13)による現状把握:
- 対戦基盤: 静的評価比較・自己対戦は既存(`bench/edax-compare/`)だが、Edaxとの実対局ハーネスは未実装。Edaxはバッチモード`-solve`のPV先頭手パースで1手ずつ指させられる見込み(未検証)。
- ギャップ候補(影響大の推測順): (1)評価関数の精度(22パターン/6クラス・WTHOR114万局面、Edax比MAE約5.06石、序盤符号一致率37.5%が突出して低い)、(2)終盤ソルバー高速化不足(安定石カット・浅空き専用関数なし、NPS実測285万 vs 設計目標8-15M)、(3)MPC既定オフ(評価の深さ間分散σ350-660cdが根因)、(4)ムーブオーダリング単純(killer/history無し、ETC削減2.2-2.6%どまり)、(5)CPU対局に定石ブック未接続、(6)アスピレーションウィンドウ無し、(7)並列化なし(フェーズ7で見送り済み)。
- 進め方: T082(Edax自動対戦ハーネス+レベル別対戦+フェーズ別ロス分析)で弱点を実測 → その結果を添えて codex-design(gpt-5.6-sol)に強化方針を諮問 → 改善タスク群に分解・実装、のサイクルで進める。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T082 | Edax自動対戦ハーネス構築 + レベル別対戦・弱点分析レポート | implementer | done | 0 |

- 2026-07-14: **T082 done**(verifier全項目合格: スモーク完走・レポートと生データの完全一致・ロス符号規約をEdax直叩きで独立再現・Actions成功・作業ツリー清潔)。**結果**: Edax level1に16勝4敗(+15.4石)、level5/10に全敗(-29.1/-37.2石)。フェーズ別ロスは中盤(21-40手目)が平均+2.22石/手と突出(序盤+0.31、終盤±0.00)。大ロス局面はX/Cマスへの自殺的着手が多数(最大+28石)。codex-review(最終レビュー)はbenchスクリプトのみでアプリ挙動に影響しないため省略(軽微タスク省略規定)、ただし弱点分析のロス符号規約の妥当性確認はverifierの確認項目に含めた。
- 2026-07-14: 対戦実測データを踏まえ、codex-design(gpt-5.6-sol)へエンジン強化方針を諮問。初回実行は要約のみだったため全文出力指示で再実行し、詳細レポート取得(`tasks/design/T083-engine-strengthening-report.md`、初回要約は`-run1-summary.md`)。
- **設計レポートの核心(2026-07-14)**: 現行の対局経路(ハーネス・アプリとも)は`search_all_moves`で時間予算を全候補に分割しており(1候補約100ms)、**single-rootのPVS探索で1秒使った実力は未計測**。0勝20敗はまだ評価関数の限界を示さない。ロードマップ: T084(計測補正・single-root化・テレメトリ・オラクルロス修正)→T085(exact切替・時間管理: 空き19-24の大ロス帯対策)→T086(TT置換規則の深度保護)→T087(Pattern v3: edge+2X中心、+約3MB)/T088(学習法: Huber・early stopping・データ分割改善、v2特徴のままablation)→T089a(history+aspiration)/T089b(hot-path)→T090(Edax教師蒸留・最終評価)。**T084完了までは対局結果を施策の採否判断に使わない**。却下: X/C固定罰則(隅が取られた局面ではX/Cが最善になりうる)・MPC再有効化(評価分散改善まで)・WASM Threads・opening bookでの弱点隠蔽。確認事項3点(重み上限8MB/Edax教師データローカル生成可/20-60-200局プロトコル)はオーケストレーターが既存方針との整合を確認して承認済み。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T084 | ベンチ補正: single-root探索導入+テレメトリ+オラクルロス修正+固定opening | codex(gpt-5.6-sol) | done | 1 |
| T085a | exact切替とノード数予算管理の再設計(TTドメイン分離・baseline-first・exact quota) | codex(gpt-5.6-sol) | review | 0 |
| T091 | Codexラッパーのログ収集修正(stderr進捗の逐次記録、tail可能化) | implementer | todo | 0 |

- **T085a検証中間結果(2026-07-14)**: codex-review は**不合格**(`tasks/review/T085a-exact-node-budget-codex-review.md`)。ブロッカー2件: (1)exact quota 25/40/60/75%比較が未実施(60%固定)、(2)コーパスが空き19〜24のみで要求の13〜30を未カバー。中3件: 木内部ExactQuotaがfallbackReason未反映、quota切れ→中盤継続+TT非混入の直接テスト不足、性能数値がコミットに未含有。いずれも固定コーパス上の短時間作業で対応可能(120局ベンチ再実行は不要=ユーザー方針と両立)。主要実装の正解値破壊は無しと確認済み。verifierの結果と統合してredoフィードバックを作成予定。
- **T091起票(2026-07-14、ユーザー指摘)**: codex-task.ps1 のログが最終メッセージのみ・完了時一括で「tailで進捗確認」が機能していない(進捗はstderrに流れるがTee-Objectはstdoutのみ捕捉)。修正タスクを起票済み。**T085aの検証完了直後に委譲する**(verifierの作業ツリー清潔チェックと並行させない)。
- **T085a実装完了(2026-07-14、Codex gpt-5.6-sol・約55分、代行コミット 05b5267)、verifier+codex-review並列検証中(範囲 cf57b56..05b5267)**。実装者報告の数値: 空き19〜24固定18局面で平均regret 5.56→2.11石(62%減)・loss>=4石 13→6件(53.8%減)、cargo test 148件パス、FFO #40-44正解値・ノード数(合計1,299,102,329)完全不変、budget-regression 18/18決定的一致・overshoot<=1024。**要注意**: 序盤単独regretが+0.253石(丸め前)で性能ゲート「悪化0.25石以内」のボーダー — 設計書§1.10の規範が序中盤合算(2.20→1.91で改善)か序盤単独かの解釈をverifierに確認させ、判定に反映する。TT分離は16-byte entry維持のためdepth上位bitにdomainを符号化する工夫あり(素直な追加だと容量半減・FFOノード増を実測で確認したとのこと)。
- **T084 done(2026-07-14)**: verifier 全10項目合格。cargo test 141件パス・FFO #40-44正解値一致(ad88c91以後の実測、codex-reviewブロッカー2を実証解消)・120局全件が空き0の真の終局(致命バグ完全解消、バグ再現局面での直接因果確認込み)・ロス362件負値0・決定性2種(fixed-depth 40局面/node-budget 10局面)とも一致・着手単位resumeを強制kill再現で実証・Actions成功・作業ツリー清潔。codex-review不合格の残指摘はユーザー承認の方針(ベンチ打ち直しは致命バグ時のみ)により T085a(回帰テスト)/T085b(resume厳格化・アトミック保存)へ振り分け済み。作業ログ最終追記の欠落は成果物完備のため不問。**確定標準ベンチ結果: single-root L10 0勝20敗(-35.8)/L5 4勝16敗(-21.6)/L1 14勝6敗(+16.5)。最大弱点=空き20〜24で平均到達深さ3.0・タイムアウト100% → T085aで対処**。
- **T084 redo#1 検証中間結果(2026-07-14)**: codex-review は**不合格**(レポート: `tasks/review/T084-bench-single-root-telemetry-codex-review.md`)。ブロッカー2件: (1) `try_resume()` が runKey(設定値)しか比較せず、エンジン/ハーネス/バイナリが変わっても旧チェックポイントを継承し、metaを現在のコミット情報で上書きするため**異なる実装の結果が現在コミット産に見える**(eval_cli.exe存在時に再ビルドしない点も同根)。(2) ad88c91 のエンジン変更後に cargo test / FFO を実行した証拠が作業ログにない。中2件: exact時間切れフォールバック経路の直接回帰テストなし、チェックポイント書き込み非アトミック。**合格確認済み部分**: 120局とも真の終局・move:null途中終了0・ロス362件負値0・決定性2種とも2回一致・git status清潔。verifier の結果(実際に cargo test/FFO を再実行するのでブロッカー2の実証データになる)と併せて redo フィードバックを作成予定。
- **ユーザー指示(2026-07-14 T084検証中)**: 「120局打ち直して弱点分析するのは時間がかかりすぎるので、致命的バグでなければやめる。それより早くエンジンを強くしたい」。→ 判定方針を変更: codex-review のブロッカーは現行ベンチ結果の有効性を否定するものではない(120局とも真の終局・ハッシュ整合を codex 自身が確認済み)ため、**ベンチ再実行は行わない**。verifier の cargo test/FFO 実測が通れば T084 を done とし、resume厳格化・アトミック保存・フォールバック回帰テストは T085a に前提修正として畳み込む(T085a はどのみちベンチを使うため、そこで堅牢化してから A/B を回す)。以後も「ベンチ全再実行」を要求するのは結果の有効性が実際に汚染された場合のみとする。
- **T084 redo再開(2026-07-14)**: ユーザー承認により追加要件「`eval_cli best --max-nodes`(ノード数予算打ち切り・決定論、smoke10局面で2回実行一致を機械検証)+ハーネスへのオプション疎通」をフィードバックに追記(ノード予算での対局60局は後続タスクのA/Bで実施)。難易度ルーティング基準1・3・4に該当するため、新構成の初回実地テストとして Codex gpt-5.6-sol(codex-task.ps1)に委譲。
- **Codexワーカー初回実地テストの結果(2026-07-14)**: 実装・検証は約40分で完了(フォールバック修正・--max-nodes・fail-fast・着手単位resume・provenance強化、cargo test 141件・FFO全問一致・決定性検証済み、スコープ規律も良好=途中で触った他ファイルを自分で復元)。**判明した制約**: Codexサンドボックスは`.git`書き込み不可でコミット不能 → コミットはオーケストレーター代行をルール化(CLAUDE.md/AGENTS.md反映済み、コード本体は ad88c91 で代行コミット済み)。残りの機械的作業(クリーンツリーでの120局ベンチ再実行+成果物コミット+push)はimplementerに委譲中。

- **T084 redo #1(2026-07-14)**: verifier(不合格)とcodex-review(redo判定)が独立に同じ致命バグを検出 — **single-root対局の80%(60局中48局)が空き18〜19で途中終了**していた。原因: exact試行が時間切れでdepth1も完走できないと`search_with_eval_inner`が`best_move=None`を返し、ハーネスが「終局」と誤解釈。**したがって「level10勝率0%→40%」の数字は汚染データであり意思決定に使用禁止**(level1の逆転現象もこのバグの交絡の可能性大)。修正指示: エンジン側の合法手フォールバック(最小限)+ハーネス側のmove:nullエラー化+loss resume粒度+provenance+ベンチ全再実行。codex-reviewの「openings.json不正JSON」指摘はverifierが両パーサで再現せず誤検知と切り分け。cargo test/FFO不変/テレメトリ/決定性/ロス非負/resume(対局側)は合格済み。**検証プロセスの成功例**: 実装者の自己申告では見えなかった致命バグを、verifier(生データの空きマス分布分析)とcodex-review(コード読解)が独立検出した。

- **T084実装完了(2026-07-14、コミット ba1b834)、verifier+codex-review並列検証中。核心結果**: single-root化により Edax level 10 勝率 0%→40%(+41.95石スイング)、level 5 5%→40%。T083設計レポートの仮説(全敗の主因は対局経路の時間分割)を実証。オラクルロスは255件全件>=0(旧方式は345件中95件が負)。**不可解な点: level 1 では allmoves 65% > single-root 30% と逆転**しており要調査(T085のexact境界問題がsingle-rootに強く効く仮説あり、テレメトリで判断)。実装者申し送り: 弱点分析はsingle-root負け局のみ、比較実行はsmoke openingセット使用(primary 30局面はT085以降のgo/no-go判定用に温存)、allmovesテレメトリはdiscDiff/typeのみ。

- **ユーザー方針(2026-07-14)**: 探索の制限方式を壁時計ベースから**ノード数予算ベース(決定論的)**に変更する。深さベース単独は不採用(同一深さでも局面により計算量が数百倍以上変わり、完全読み連鎖で1手20分の実測例があるため)。壁時計は普段発動しない大きめの保険としてのみ残す。T085(exact切替・時間管理)のスコープに組み込み、ベンチで壁時計1秒と同等コストのノード予算をA/B比較して棋力非劣性を確認してから切り替える。アプリのCPU対局への配線も同方式で行う。

## タスク一覧(フェーズ1: エンジン核)

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T001 | Rust ワークスペース初期化 + wasm-bindgen 疎通確認 | implementer | done | 0 |
| T002 | ビットボード表現・合法手生成 | implementer | done | 0 |
| T003 | Zobristハッシュ + 置換表(TT) | implementer | done | 0 |
| T004 | 軽量評価関数 v1(モビリティ・安定石・隅・パリティ) | implementer | done | 0 |
| T005 | 探索エンジン(反復深化+PVS/NegaScout+置換表統合) | implementer | done | 0 |
| T006 | 終盤完全読みソルバー | implementer | done | 0 |
| T007 | 探索エンジンの補強(TTスケール混同防止・終局ロジック重複解消) | implementer | done | 0 |
| T008 | WASM API(Engine構造体 + JSON解析プロトコル) | implementer | done | 0 |
| T009 | FFO endgame testベンチマーク + NPS計測(フェーズ1完了条件) | implementer | done | 1 |

## タスク一覧(フェーズ2: GitHub Pagesで遊べる対局アプリ)

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T010 | /app フロントエンド雛形(Vite+Preact+TS) | implementer | done | 0 |
| T011 | Canvas盤面描画・ゲーム状態管理 | implementer | done | 0 |
| T012 | Web Worker + WASMエンジン統合 | implementer | done | 0 |
| T013 | 対局モード統合 | implementer | done | 0 |
| T014 | PWA設定 | implementer | done | 0 |
| T015 | GitHub Pagesへのデプロイ設定 | implementer | done | 0 |

依存関係: T010 → {T011, T012 並列可} → T013(T011+T012統合)→ T014(PWA)→ T015(デプロイ、実際にpush・Actions実行・公開URL確認まで行う)

## タスク一覧(フェーズ4: モード分割 + 言語化支援)

2026-07-08 ユーザー追加依頼。設計書記載順(定石練習→中盤練習→詰めオセロ→棋譜解析)で実装。各モード共通で「評価値即時表示+ソース色分け(定石/中盤/終盤)」「悪手判定(閾値ユーザー調整可)」「悪手理由表示(言語化支援)」「レスポンシブ」が必須。

### 4-A. 定石練習モード

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T016 | 定石データの調査・収集(公開情報から独自DB化) | implementer | done | 2 |
| T017 | 定石DB構造化(DAG+8対称正規化ハッシュ) | implementer | done | 0 |
| T018 | 複数候補手一括評価API(全モード共通基盤) | implementer | done | 1 |
| T019 | 共通UI: 評価バッジ(ソース色分け)+悪手判定設定 | implementer | done | 0 |
| T020 | 定石練習モード本体(ゲームロジック+SRS+UI+ナビゲーション) | implementer | done | 1 |

**フェーズ4-A(定石練習モード)完了(2026-07-08)。** 実データで25手セッションのクリア・悪手判定とも実機確認済み。

### 4-B. 中盤練習モード

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T021 | 中盤練習モード本体(局面生成+判定モード+相手強さ+UI+ナビゲーション拡張) | implementer | done | 1 |

### 品質検証(横断)

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T022 | 評価関数の妥当性検証(Edaxとの比較基盤構築+初回比較) | implementer | done | 0 |
| T023 | Service Workerキャッシュのバージョン自動更新(本番キャッシュ固着バグ修正) | implementer | done | 1 |
| T024 | 評価関数の出力スケールをEdax(実石差)に近づける較正 | implementer | done | 0 |
| T025 | 定石データの大幅拡充(虎系のローズビル・酉等、既存35ラインでは不足) | implementer | done | 0 |
| T026 | 定石練習クリア画面で「到達した具体的な終端」を明確に表示 | implementer | done | 0 |

### 4-C. 詰めオセロ

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T027 | 詰めオセロ問題データ生成パイプライン(唯一解性フィルタ+難易度スコアリング) | implementer | done | 0 |
| T028 | 詰めオセロプレイモード本体(出題UI+完全読み判定+成績記録+デイリー) | implementer | done | 0 |

T027はengine/配下を触る可能性があり、T024(同じくengine/を編集中)と競合するため、**T024のコミット完了後に着手**する。

### 4-D. 棋譜解析

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T029 | 棋譜解析モード(入力+解析パイプライン+評価グラフ+ナビゲーション拡張) | implementer | done | 0 |
| T030 | 棋譜解析の悪手分析パネル(比較PV+フリー分岐探索+理由表示+練習送り) | implementer | done | 0 |

T029はT028のverifier/reviewer合格(app.tsx・appDb.tsの状態確定)後に着手する(タブ構成・DBバージョンの競合回避)。T030はT029のverifier/reviewer合格後に着手する(AnalysisMode.tsxの状態確定を待つ)。

**フェーズ4(モード分割: 定石練習・中盤練習・詰めオセロ・棋譜解析+悪手分析)完了(2026-07-09)。設計書§2〜6の主要モードがすべて本番稼働中。**

## タスク一覧(フェーズ6.5: 言語化支援レイヤー、`othello-trainer-design-verbalization.md`)

explorerによる設計書精読の結果、以下が判明(2026-07-09):
- §1特徴量層(12特徴量)・§4モチーフ検出(19種)は具体的な定義があり実装可能。
- **§2評価内訳分解層は「46パターングループ」を前提にしているが、これは本プロジェクトが後回しにしているWTHOR学習ベースのパターン評価関数(フェーズ3)を要求しており、現行の3項(モビリティ・隅・安定石)線形評価関数では実現できない。** 46パターンの具体的定義も設計書内には無い。→ T031でスコープを「現行3項への厳密な分解」に縮小して対応し、フェーズ3実装後に拡張する方針とする。
- §4モチーフのうち「爆弾」「隅絡みの一方通行」の2種は検出条件が設計書に未記載(定義なし)。実装時に判断が必要。
- §9 LLM解説層はBYOK方式・入力を構造化データ(特徴量・寄与分解・回収点・タグ・PV)に限定しハルシネーション対策する設計が明記されている。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T031 | 特徴量層(12特徴量)+ 評価内訳分解層(現行3項評価のwaterfall分解) | implementer | done | 1 |
| T032 | モチーフ検出タグ + 盤面可視化オーバーレイ | implementer | done | 0 |
| T033 | 反証層(回収点検出: 寄与が急変した手の特定) | implementer | done | 0 |

想定タスク分割: T032(done)→T033(done)→T034(緊急バグ修正、done)→T035(言語化トレーニングモード、done)→T036(用語集+概念別弱点統計、done)→T037(任意LLM解説層、BYOK、done)。

**フェーズ6.5(言語化支援レイヤー)完了(2026-07-09)。** T031〜T037すべてdone。特徴量層(12特徴量)・評価内訳分解層(3項waterfall)・モチーフ検出タグ(15種)・盤面オーバーレイ・反証層(回収点検出)・言語化トレーニングモード(予測→理由宣言→検証+二択比較ドリル)・用語集+概念レッスン+弱点統計ダッシュボード・任意LLM解説層(Anthropic Claude API、BYOK)がすべて本番稼働中。T037はAPIキーの取り扱いをverifier/reviewer双方が重点的にセキュリティ確認し、問題なし(実キーのコミットなし、localStorage限定保存、Anthropic公式エンドポイントのみへの送信)。

**設計書(`othello-trainer-design.md`のフェーズ2・4・6・6.5、`othello-trainer-design-verbalization.md`全体)が完了。** 残るはフェーズ3(WTHOR学習によるパターン評価関数)・フェーズ7(磨き込み: マルチスレッド・重みv2・UI仕上げ)。これらは規模・性質が大きく異なる(特にフェーズ3はWTHORデータ入手可否から始まる不確実性の高い作業)ため、次の一手をユーザーに確認する。

**2026-07-11 追記: 設計書ロードマップ完了。** フェーズ3(WTHOR学習、T040〜T045)・盤面評価オーバーレイ(T039/T042)・エンジン強化(MPC/ETC/終盤パリティ、T048/T051/T052)・UI/UX磨き込み優先度8項目(T061, T065〜T071)・ダークモード対応(T068)がすべて完了。フェーズ7の残り項目「マルチスレッド化(探索エンジンの並列探索)」について、現状の探索性能(単一スレッドNPS約160〜290万、FFO終盤問題全問正解)で実用上支障が無いこと、WASM Threads化にはT014で明示的にスコープ外とした COOP/COEP ヘッダの再導入(過去にService Worker関連の不具合が複数回発生した領域、T023・T062)が必要になることを踏まえてユーザーに確認した結果、**「実装しない」を選択(2026-07-11)**。これにより`othello-trainer-design.md`・`othello-trainer-design-verbalization.md`のロードマップは実質的に完了とする。
- T036完了。用語集18項目(T032モチーフ15+T031寄与タグ3)を実装。例局面・反例局面は静的な手作りをせず、出題プールから実際の検出ロジックで動的検索する方式(誤った例を載せるリスク回避)。概念別弱点統計は`verbalizeAttempts`のみを集計源とする近似実装(中盤練習・棋譜解析へのタグ付け拡張はスコープ外として明示)。reviewerの非ブロッキング指摘: `analysis`→`verbalize`という新しい依存方向が発生(逆向きの既存依存と合わせ将来の循環依存リスク)、ダッシュボード文言が設計書の「負けの42%」形式ではなく「理由正答率」形式に簡略化。
- T035完了。「手を選ぶ→理由タグを宣言する→手と理由を別々に2×2採点」の言語化トレーニングモードを実装(6タブ構成completed)。T031の評価内訳分解+T032のモチーフ検出を理由タグの正誤判定に再利用。IndexedDBバージョン4→5移行もマイグレーション実地検証済み。reviewerの非ブロッキング指摘: 「中盤練習プール」/「自分の悪手局面」が現状データでは同一集合になる(将来source多様化まで暫定)。二択比較ドリルの2×2全パターン実機再現は未実施(単体テストではカバー済み)。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T035 | 言語化トレーニングモード(予測→理由宣言→検証+二択比較ドリル) | implementer | done | 0 |

### 【緊急】品質問題

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T034 | 棋譜解析でWASM環境のtime_ms予算が機能せず数分間ハングする不具合の調査・修正 | implementer | done | 1 |
| T038 | 棋譜解析モードへの定石DB連携(定石内の手を悪手誤判定から除外) | implementer | done | 0 |

**T033のverifierが本番環境で発見(2026-07-09)**: 24〜30手程度のごく普通の合法棋譜を「棋譜解析」モードで解析すると、最初の1手の解析だけで6分以上応答が返らない現象を確認(8手棋譜は23秒で正常完了)。`engine/src/search.rs`のネイティブテストでは`time_ms`予算が2秒以内に守られることを確認済みだが、本番のWASM実行環境では機能していない可能性が高い。棋譜解析モード(T029〜T033)全体の実用性に関わるため、言語化トレーニングモード(旧T034、T035に繰り下げ)より優先して修正する。

- **T034完了(1回のやり直し後)。** 根本原因は「WASM固有の問題」という当初仮説ではなく、**ネイティブでも再現するロジックバグ**だった: 反復深化の時間予算チェックが「1つのdepth完了ごと」という粗い粒度でしか行われておらず、探索木内部で無条件・無制限に呼ばれる`endgame::solve_exact`(素朴な完全読み、空き22前後でも最悪75〜116秒かかりうる)が時間予算を一切考慮していなかった。修正: `negascout`/`negamax`の再帰内で1024ノードごとに時間予算をチェックし、超過時は安全にフォールバックする`solve_exact_bounded`を新設。26手棋譜の再現ケースで修正前「30秒以上応答なし(本番では6分以上)」→修正後「1.68秒」に改善、本番環境でも26手棋譜の解析が41秒程度で完走することを確認。
  - やり直し理由: 修正により`search()`(単体API)がタイムアウトで静的評価にフォールバックした際、`protocol.rs`の`score.type`が事前計算値のまま「exact(完全読み)」と誤表示される新規回帰をreviewerが発見(verifierは検出できず)。`BlunderPanel`のフリー分岐探索で、重い局面がタイムアウトすると「確定」の緑バッジで実は粗い静的評価が提示される実害があった。T018の`MoveEval::is_exact`と同じパターンで`SearchResult::is_exact`を追加し解消。
  - **教訓**: 探索エンジンに「タイムアウト時のフォールバック経路」を追加する際は、フォールバック後の結果の「exact/midgameラベル」等のメタデータも実態に追従するよう必ず一緒に確認すること(T018で一度学んだのと同種の教訓が、新しいフォールバック経路で再発した)。

- **T038完了(2026-07-09、ユーザー報告契機)**。ユーザーが棋譜解析モードで「酉定石」を解析した際、定石内の手にもかかわらず評価値が乱高下し「??悪手」「(逆転)」と誤判定される問題を報告。調査の結果、棋譜解析モード(`analyzeGame.ts`)が定石DBを一切参照していないことが原因と判明(ロジック自体にバグはなし)。ユーザー選択により定石DB連携を追加: 定石ラインに乗っている手は評価ソース「定石」表示になり、悪手・逆転判定から除外(`classification:'best'`・`lossDiscs:0`・`reversal:false`に上書き)、定石を外れた以降は従来通りの評価に復帰する。verifier・reviewerとも合格(本番URLでのPlaywright確認済み、酉定石13手+酉フック2手が「定石」表示・悪手タグなし、16手目の定石外の手で通常評価へ即復帰することを実機確認)。reviewerの非ブロッキング指摘: (1) `types.ts`のコメント誤字「評価ソード」、(2) LLM解説層(T037)は`evalSource`/定石名をまだ入力に使っておらず「定石だから最善扱い」という理由をLLMに説明させられない(将来タスクで検討)、(3) `EvalGraph.tsx`のグラフ帯色分けが定石区間専用色を持たない(表・パネルのみが今回のスコープ)。なお、評価値の乱高下自体(ヒューリスティック評価関数の序盤精度限界)は未解決で、根本解決にはフェーズ3(WTHOR学習)が必要。

- T032完了。モチーフ検出タグ15種(良い手6・悪い手8・罠系1「ストナー」)+盤面オーバーレイ4種(フロンティア石/確定石/種石/危険なX・Cマス)を実装。「爆弾」「隅絡みの一方通行」は設計書に検出条件の記載が無いためT016の教訓により未実装(推測・創作を回避)。reviewerの非ブロッキング指摘: 「種石作り」の逆方向判定(`computeVulnerableSeeds`)がRust側`compute_seed_stones`とロジックを重複実装しており、T031と同種のdriftリスクを持つ(現時点で乖離は確認されずbugではないが、将来Rust側変更時に追従漏れの可能性)。将来この領域に手を入れる際はRust側から計算済み値を返す設計への統一を検討。
- T031完了(1回のやり直し後)。特徴量層(12特徴量、Rust `explain.rs`)+評価内訳分解層(現行3項評価へのスコープ縮小、waterfall表示)を実装。やり直し理由: (1) 特徴量10「余裕手」が未実装のまま完了報告(未承認のスコープ縮小)、(2) TS側の重み定数複製について「テストでdrift検証済み」との説明が虚偽(実際は同じ値同士を比較する循環参照テスト)。修正: 特徴量10をTS側実装、重み定数の二重管理そのものを解消(Rust側`evalTerms`が加重後3項を直接返す設計に変更、TS側は重み定数を一切持たない)。**教訓: 「テストで担保している」という主張は、実際にテストコードを読んで独立検証になっているか確認すること(自己参照的なテストで安心してしまうリスクがある)。またRust/TS間で計算ロジック(特に定数)を複製する設計は、テストでカバーするより複製自体をなくす方が根本解決になりやすい。**

- T029完了。棋譜解析モード(トランスクリプト入力・盤面手動並べ、終局側からの解析パイプライン、悪手分類、評価グラフ、IndexedDBキャッシュ)を実装、5タブ構成に。実装中に「depth18時間無制限だと初期局面で60秒超かかる」問題を発見し、時間予算(1500ms)を追加して解決(過去のFFO/Edaxハング事故と同種のリスクを事前に回避、完全読み区間の精度には影響しないことをreviewer/verifierがsearch.rsのソースで確認)。IndexedDBバージョン4への移行もマイグレーション実地検証済み。reviewerの非ブロッキング指摘: 「逆転悪手だがロス閾値未満」という稀なケースでグラフ上のマーカー表示とムーブリスト表示が非対称になる軽微な表示不整合あり。次の言語化支援タスクで併せて検討。
- **設計書フェーズ4(モード分割: 定石練習・中盤練習・詰めオセロ・棋譜解析コア)完了(2026-07-09)。** 残るは棋譜解析の悪手分析パネル(T030)と、フェーズ6.5(言語化支援レイヤー全体)。

- T028完了(4タブ構成完成: 対局/定石練習/中盤練習/詰めオセロ)。IndexedDBバージョン2→3移行(`tsumeAttempts`ストア追加)を、verifierが`fake-indexeddb`で実際のマイグレーションシナリオ(既存データ保持+新ストア追加)を再現し安全性を確認済み。T021の教訓(バージョン不整合による破壊)が再発しないことを実証。**フェーズ4(定石練習・中盤練習・詰めオセロ)が完了、残るは4-D(棋譜解析)。**
- **フェーズ4-C(詰めオセロ)完了(2026-07-09)。**

- T024完了。ユーザー要望「評価値をなるべくEdaxに寄せたい」に対応。80局面の特徴量とEdax評価値を線形回帰し重み較正(`MOBILITY_WEIGHT`10→253、`CORNER_WEIGHT`2500→1088、`STABLE_WEIGHT`1500→93)。スケール比5.73倍→1.06倍に改善(理論上限±64以内に収まる割合46%→96%)。FFO完全読みは無影響、自己対戦24局で明確な棋力低下なしを確認。reviewerの非ブロッキング指摘: 回帰計算コード自体が未コミット(データのみ)、24局は統計的検出力が低い、corner/stable間の多重共線性による重要度逆転は要監視、EvalBarのCLAMP=16は較正後も上限張り付きが残る場合がある。将来のポリッシュタスクで検討。
- **重要な運用教訓(2026-07-09)**: T024の実装エージェントが、作業自体は正しく進めていたにもかかわらず、長時間(最大23時間)無言で停止する事象が複数回発生した(`SendMessage`で再開すると「had no active task」と判定され停止が確認された)。CLAUDE.mdに「エージェントの生存確認」ルールを追加済み(委譲後30分程度通知が無ければファイル更新時刻を確認し、必要なら`SendMessage`で状況確認・再開する)。今後の長時間タスクではこの生存確認を徹底する。
- 2026-07-09 ユーザー指示により、フェーズ4-C(詰めオセロ)以降、設計書の全機能完成まで自律的にタスク継続中(CLAUDE.md「自律継続ルール」参照)。
- T027完了。詰めオセロ問題データ生成パイプライン(Rust CLI`puzzlegen`で完全読み評価+タイムアウトガード、TypeScript側で唯一解性/明確さフィルタ・難易度スコアリング・タグ導出・デイリー選択の純粋関数)。348候補→182問採用(通過率52.3%)、難易度5段階(41/32/41/32/36)。verifier/reviewerとも合格、182問全件のデータ整合性(唯一解性・明確さ・符号)を機械検証済み。次はT028(プレイモードUI)。

## タスク一覧(ユーザー追加依頼 2026-07-09: 盤面評価オーバーレイ + フェーズ3(WTHOR学習)着手)

ユーザーより2件の追加依頼: (1) 盤面上の各合法手マスに評価インジケータ(良い手=プラス/悪い手=マイナス)を表示する機能、全モード(対局含む)に導入・デフォルト非ON。(2) 評価関数をEdaxにさらに近づけたい→本格的にフェーズ3(WTHOR学習)に着手。

- WTHORデータは調査の結果FFO公式で今も無料配布中だが利用規約明記なし(グレーゾーン)。ユーザー方針: 実データは学習時のみローカル使用しリポジトリにコミットしない(`.gitignore`)、学習済み重みファイルのみコミット対象とする。
- 盤面オーバーレイは既存のBoardOverlay(T032モチーフ検出)と同じ実装パターンで追加可能。まず共通コンポーネント+対局モード統合(T039)を実施し、合格後に残りのモード(定石練習・中盤練習・詰めオセロ・棋譜解析)へ展開する後続タスクを起票する。
- **T039完了(2026-07-09)**。共通コンポーネント`MoveEvalOverlay`(BoardOverlayと同パターン)+設定永続化(`moveEvalOverlaySettings.ts`)+対局モード統合。verifier・reviewerとも合格、本番URLで実機確認済み(候補手マスへの色分けインジケータ表示、クリック貫通、ON/OFF・リロード後の永続化)。タスク仕様書の背景説明に軽微な誤記(`BlunderConfig`を使うつもりだったが実際に`classifyMove`が要求するのは`ClassifyThresholds`)があり、implementerが既存の`analysis/thresholdSettings.ts`(棋譜解析モードと共有)を再利用する形で正しく解決(verifier・reviewerとも技術的に妥当と判定)。非ブロッキング指摘: (1) 着手後評価と着手前オーバーレイ評価で`requestAnalyzeAll`が人間の一手ごとに実質2回呼ばれる(想定内、将来の最適化候補)、(2) 対局モード画面内に閾値設定への導線が無く、変更するには棋譜解析モードの設定画面を操作する必要がある(発見しにくいが機能はする)。残りのモード(定石練習・中盤練習・詰めオセロ・棋譜解析)への展開は次のタスク(T042想定)で対応。
- **T042完了(2026-07-09)**。T039の共通部品(`MoveEvalOverlay`等、無変更)を定石練習・中盤練習・詰めオセロ・棋譜解析(フリー分岐探索)の4モードに展開。各モード固有の人間手番検知・二重リクエスト防止ガードを踏襲。verifier・reviewerとも合格、本番URLで4モードすべて実機確認済み(オーバーレイ表示・着手進行・設定のモード間引き継ぎ)。デプロイCIが初回「ホストランナー確保失敗」で失敗したが、GitHub側の一時的インフラ問題と確認しリランで解消(コード起因ではない)。reviewerの非ブロッキング指摘: 棋譜解析モードの「詰めオセロとして解いてみる」即席判定盤面にはオーバーレイを適用していない(タスク文がフリー分岐探索を主眼にしていたための解釈、実装ミスではない)。**盤面評価オーバーレイ機能(全5モード)完了。**
- フェーズ3はT040(WTHORデータ取得+パーサー実装)を皮切りに、パターン特徴量設計・学習パイプライン・engine/eval.rsへの統合+Edax/FFO比較検証、と複数タスクに分割して段階的に進める。
- **T040完了(2026-07-09)**。`train/`クレート新設。一次情報(LimeEng/wthorのRustソース)を実際に取得しフォーマット仕様(ヘッダ16バイト・ゲームレコード68バイト・着手バイト`10*行+列`)を確認した上で実装。FFO公式サイトから2023・2024年データ(計5,369ゲーム・321,321手)を実際にダウンロードし全件パース・合法性検証に成功(`train/data/`は`.gitignore`で除外、コミット対象外)。verifier・reviewerとも合格(独立クロスチェックとしてPythonで生バイトを別途手動デコードし一致確認済み)。reviewerの非ブロッキング指摘: (1) 着手バイト`0`(パディング)以降に非`0`バイトが出現した場合の異常データ検出が`continue`のみで`break`/明示エラーではない(未知データでの潜在リスク)、(2) `num_games`(ファイル由来の非検証入力)由来のサイズ計算が32bit環境でオーバーフローしうる。次はパターン特徴量設計・学習パイプライン(回帰)のタスク化に進む。
- **T041完了(2026-07-09)**。22パターン(行8・列8・主/反対角線2・隅3x3ブロック4、すべてループ演算で機械的生成、手書きセルリストなし)+ステージ分割(空きマス数/5、13段階)+オンラインSGD回帰(L2正則化)を`train/`クレートに実装。学習データはWTHOR2015〜2024年の10年分(19,119対局・約114万局面)まで拡張(2年分では過少と判断し追加ダウンロード、`train/data/`は引き続き非コミット)。学習済み重み`train/weights/pattern_v1.bin`(約10MB)をコミット、フォーマット仕様は`train/weights/README.md`に明記。ホールドアウト誤差(MSE471.22/MAE16.66)がベースライン(常に0予測、または常に訓練平均予測。MSE714前後/MAE22前後)よりMSEで約34%・MAEで約24%小さく、学習が実信号を捉えていることを確認。verifier・reviewerとも合格(対局単位でのホールドアウト分割によりデータリークが無いこと、重みファイルのバイナリレイアウトがREADME記載と一致することを独立検証済み)。reviewerの非ブロッキング指摘: (1) `train_data.rs`のリプレイ・パス処理ロジックが`wthor.rs::replay`の複製で将来ドリフトのリスク、(2) `train_data.rs`のテストが自己整合性検証中心で既知の絶対値によるテストがない、(3) 作業ログの新規テスト件数記載がやや不正確(37件はtrain crate全体、T041新規は25件)。次はengine/eval.rsへのパターン評価統合(WASM組込み)+Edax/FFO比較検証のタスク化に進む。
- ユーザー方針(2026-07-09): T043の結果(Edaxに近づいたが自己対戦で負け越し)を受け、WASM配線の前に対称重み共有によるモデル改善(T044)を先に行う方針を選択。
- ユーザー方針(2026-07-10): T044の結果を受け、パターン評価v2をWASM/アプリに配線し新デフォルト評価にする方針を選択(T045)。
- **T045完了(2026-07-10)、フェーズ3(WTHOR学習パターン評価)一連のタスク完結**。`Engine`(WASM)に`load_pattern_weights`を追加し重み読み込み時は`search_with_eval`/`search_all_moves_with_eval`経由でパターン評価v2を使用、未読み込み時は旧3項評価にグレースフルフォールバック。NPS比較(旧2,290,357 vs パターンv2 1,624,296、比率0.71)を実測し、T034の教訓(時間予算チェック粗さ)に照らして問題ない水準と確認。終盤完全読みは無変更(影響なし)。本番URLで実機確認(対局モードの評価値がパターンv2由来であることをeval_cli独立計算と突き合わせて確認、オフライン動作、フォールバック動作も個別に検証)。verifier・reviewerとも合格。**reviewerの重要な非ブロッキング指摘**: `engine/src/explain.rs`(T031の評価内訳分解層、悪手の「理由」説明に使用)は旧3項ヒューリスティックのままで、表示される評価値(パターンv2由来)と理由説明(旧モデル由来)がズレている可能性がある → フォローアップ検討事項として記録。他のshould指摘: cargo fmt未適用の2箇所、worker.tsのフォールバック処理に自動テストが無い(Playwright手動確認のみ)、低速回線での初回ロード体感latencyは未検証。
- **T044完了(2026-07-10)**。22パターンインスタンスを対称群(D4、8変換)のオービットで6クラス(行列4クラス+対角線1+隅1)に機械的に分類し、クラス単位で重みを共有するv2モデルを実装。パーミュテーションの正しさは「インスタンス固有抽出+パーミュテーション」と「盤面全体変換+代表インスタンス抽出」の2方式一致(200局面×2手番×22インスタンス=8800通り)で検証済み。**結果は明確な改善**: ホールドアウト誤差 MSE471→442・MAE16.66→16.08、Edax比較MAE 5.91→5.06石(旧ヒューリスティック5.96より明確に近い)、**自己対戦(24局)でv1の9勝-15敗からv2は15勝-8敗-1分に逆転**(旧ヒューリスティックに勝ち越し)。verifier・reviewerとも合格、数値は完全再現確認済み(seed固定)。`pattern_v1.bin`は比較用に無変更のまま保持、`PWV1`旧フォーマットの後方互換読み込みも維持。reviewerの非ブロッキング指摘: (1) L2正則化がクラス内複数インスタンス衝突時に多重度分だけ複利的に効く(現行l2=1e-5では無視できる程度)、(2) クラス代表選択規則の明記がコードコメントに薄い(READMEには明記済み)。**次のステップ(WASM/アプリへの配線)に進める状態。ユーザーに確認予定。**
- **T043完了(2026-07-09)**。`train/src/patterns.rs`を`engine/src/patterns.rs`に移動(T041 reviewer指摘のドリフトリスク解消、trainクレートは`engine::patterns`を参照)。読み取り専用`PatternWeights`(`engine/src/pattern_eval.rs`)を新設し既存`pattern_v1.bin`と完全互換。`search.rs`に`static_eval`/`search_with_eval`等を追加し、既存`search`/`search_all_moves`は挙動不変のラッパー化(終盤完全読みには一切影響なし、FFO #40-44全問正解を確認)。**重要な検証結果**: Edax比較(28局面)で静的評価のEdaxとの近さはわずかに改善(MAE 5.96→5.91石、符号一致67.9%→75.0%)した一方、**自己対戦(24局、公平な同一探索深さ条件)ではパターン評価側が9勝・旧ヒューリスティック側が15勝と負け越し(平均-6.21石)**。verifier・reviewerとも、探索条件の不公平(バグ)ではなく、パターン評価モデル自体の汎化性能不足(v1は対称性による重み共有が無く、訓練データに無い局面組み合わせで重みがゼロのまま)が原因という実装者の分析を支持。WASM/アプリへの配線(T044想定)に進む前に、この「Edaxには近いが実戦は弱い」という結果をユーザーに報告し、次の方針(対称重み共有等でモデル改善してから配線するか、現状のままopt-in機能として配線するか)を確認する。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T039 | 盤面セル評価オーバーレイ(候補手ごとの評価インジケータ)+ 対局モードへの統合 | implementer | done | 0 |
| T041 | フェーズ3着手(2): パターン特徴量抽出 + 回帰学習パイプライン(train/クレート拡張) | implementer | done | 0 |
| T042 | 盤面セル評価オーバーレイを残り4モードへ展開(定石練習・中盤練習・詰めオセロ・棋譜解析) | implementer | done | 0 |
| T043 | フェーズ3着手(3): パターン評価をengineクレートに統合 + Edax/FFO比較検証(Rust側のみ) | implementer | done | 0 |
| T044 | フェーズ3着手(4): パターン評価に対称重み共有を導入しモデル改善(pattern_v2) | implementer | done | 0 |
| T045 | フェーズ3着手(5): パターン評価v2をWASM/アプリに配線し新デフォルト評価にする | implementer | done | 0 |
| T040 | フェーズ3着手(1): WTHOR棋譜データ取得 + パーサー実装(train/クレート新設) | implementer | done | 0 |

## タスク一覧(ユーザー追加依頼 2026-07-10: 定石表示修正・LLM解説削除・Edax実装調査)

ユーザーより3件: (1) 棋譜解析モードで定石内の手の評価値・グラフが無意味に上下する表示をやめてほしい(T038時点の設計判断ミス)。(2) エンジンをもっと強くするため、Edaxの実装方法をちゃんとリファレンスしてほしい。(3) LLMによるAI講評/AI感想戦機能(T037)は不要なので削除。

T046とT047は両方`AnalysisMode.tsx`を編集するため直列実行(T047削除→T046修正の順)。Edax調査(explorer/research)は独立のため並列実施。
- **T046完了(2026-07-10)**。`EvalBadge.tsx`(全モード共通)は`source==='joseki'`のとき数値非表示・「定石」ラベルのみに変更。評価グラフ(`EvalGraph.tsx`)は定石区間を第3の帯色(琥珀色、全高rect描画)でフラット表示、`buildGraphPoints`で定石区間のvalueを0固定。verifier・reviewerとも合格、本番URLで酉定石16手を使い実機確認済み(1-13手目は定石表示のみ、14手目以降は通常の評価変動・悪手判定が復活)。reviewerの非ブロッキング指摘: (1) 帯に`pointer-events:none`が未指定(現状実害なしだが将来の描画順変更時のリスク)、(2) 定石区間のツールチップが常に「+0.0石」表示で実際の評価値ではないことが伝わりにくい、(3) `bandClass`/`buildGraphPoints`の単体テストが無い。verifierの補足: 定石練習・中盤練習モードはそもそも`source:'joseki'`のバッジを表示しないため、今回の変更は実質的に対局・棋譜解析モードに影響(バグではなく元々の設計)。
- **T047完了(2026-07-10)**。`app/src/llm/`ディレクトリ全体(12ファイル)を削除、`BlunderPanel.tsx`・`AnalysisMode.tsx`・`AnalysisMode.css`から参照を除去。APIキーはlocalStorageのみ使用でIndexedDBスキーマ変更は不要と判断(verifier独立確認済み)。verifier・reviewerとも合格、本番URLで他機能(比較PV・評価内訳・悪手分析パネル等)への影響が無いことを確認済み。reviewerの非ブロッキング指摘: 既存ユーザーのlocalStorageに`othello-trainer:llmApiKey`キーが残存する(データ消去まではスコープ外のため許容)。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T047 | LLMによるAI講評/AI感想戦機能(T037)を削除する | implementer | done | 0 |
| T046 | 定石内の手で評価値・評価グラフが無意味に上下する表示を修正 | implementer | done | 0 |

ユーザー方針(2026-07-10): Edax実装調査の結果、投資対効果順にMPC→ETC→終盤パリティ着手順序付けの3項目をエンジン強化として順に実装する。いずれもengine/src/を触るため直列実行。
- **T048完了(2026-07-10、正当な負の結果として確定)**。MPC(Multi-ProbCut)を実装、σ(標準偏差)を自作エンジンで実測(depth5-12、350〜660centi-disc、Edaxの想定値より1桁近く大きい)。実装過程で重大なバグ(PVノード=alpha/betaが番兵値のままの局面にカットオフが誤爆し、実際にはどの手も探索せず祖先のbeta値をそのまま返す)を発見・修正(自己対戦7勝17敗から22勝23敗3分に改善)。**しかし速度向上(NPS・到達深さ)は実証できず**(むしろ3.6〜13.3%ノード数増加)、原因はパターン評価v2の深さ間分散が大きすぎてマージンを保守的にせざるを得ずカットオフが割に合わないため。オーケストレーター承認のもと「実装・検証は正しいが既定オフ(mpc_enabledフィーチャでopt-in)」で確定。verifier・reviewerとも合格(バグ修正箇所・既定ビルドへの無影響・終盤ソルバー無改変を独立確認)。次はETC(Enhanced Transposition Cutoff)に着手。
- **T051完了(2026-07-10)**。候補手を再帰探索する前に子局面のTTを覗いてカットオフを前倒し判定する`etc_try_cutoff`を実装。MPCと異なり「探索結果を一切変えない安全な枝刈り」であることを、ETC有効/無効での探索結果完全一致テスト(26通り・18通り、いずれも不一致0件)で確認。ノード数削減率2.20%/2.64%(小さめだが確実な削減、TT2-tier構成の再訪率に起因すると分析)。FFO終盤テスト(#40-44)はノード数までT009時点の記録と完全一致(終盤ソルバー無改変を確認)。verifier・reviewerとも合格(TT判定ロジックの一致、ガード条件の正しさ、パス局面がTTに存在しないという暗黙前提の妥当性を独立確認)。reviewerの非ブロッキング指摘: (1) ETC不発時にZobristハッシュを2回計算しており削減率をやや相殺している可能性、(2) パス局面除外の理由がコードコメント未記載、(3) タイムアウト後のETC安全性の根拠が未文書化、(4) 発火回数自体を計測するテストが無い。次は終盤パリティベース着手順序付けに着手。
- **T052完了(2026-07-10)、エンジン強化3項目(MPC/ETC/終盤パリティ)完結**。空きマスの4方向連結領域分割(`empty_region_sizes`)を実装し、既存の着手順序(隅優先→相手モビリティ少ない順)の後段タイブレークとしてパリティ(奇数優先)を追加。実測でパリティを高優先度に置くと明確に悪化する(+13.7%)ことを発見し、タイブレーク配置を採用。FFO#40-44全問正解(正解値変化なし)、ノード数合計-6.70%(問題ごとにばらつきあり、#42/#43は-15〜17%、#40/#44はわずかに増加)。verifier・reviewerとも合格、数値再現確認済み。reviewerの非ブロッキング指摘: (1) 候補手が1手のみでも毎回フラッドフィルを計算しておりわずかな無駄、(2) bitboard.rsとのシフト関数重複(スコープ限定のための意図的選択)、(3) ノード増加問題の要因分析がやや不足。**MPC(既定オフ)・ETC・終盤パリティのいずれもengine/srcのみの変更で、本番WASMへのデプロイはまだ行われていない**(次のWASM再ビルド・デプロイタスクで反映される)。
- **T053完了(2026-07-10)**。調査の結果、T048/T051/T052の各pushで実は既にGitHub Actionsデプロイが自動実行・成功済みだったことが判明(`push: branches:[main]`トリガーのため)。念のため空コミットで再トリガーし、本番URLで対局・詰めオセロ・棋譜解析の3モードを実機確認(コンソールエラー0件、CPU応答122ms・詰めオセロ判定74ms・棋譜解析16手25.6秒、T034のようなハングなし)。verifier・reviewerとも合格。reviewerの非ブロッキング指摘: 検証に使った棋譜解析の棋譜が16手のみで、棋譜解析パイプライン自身の完全読み分岐(空き≤22、T034の`score.type`誤表示バグが見つかった箇所)には到達しておらず、詰めオセロでのみ完全読み経路を確認(次回40手前後の棋譜での確認を推奨)。**フェーズ3(WTHOR学習)・盤面評価オーバーレイ・エンジン強化(MPC/ETC/終盤パリティ)を含む今回依頼された全タスクが完了。**

## タスク一覧(ユーザー追加依頼 2026-07-10: 評価値表示の不安定さ調査・修正)

ユーザー報告: 「評価値の表示が最初にできなかったり、終局近くで消えたり、いろいろと不安定」。explorer調査により2つの独立した根本原因を特定:
1. 各モードがマウントのたびに新規Worker(EngineClient)を生成・破棄しており、`pattern_v2.bin`(2.7MB)の再fetch+WASM再初期化のコールドスタートが毎回発生 → 「最初に出ない」の主因。
2. 中盤練習・詰めオセロのパス処理が別useEffectで非同期に反映されるためワンテンポ遅れてオーバーレイがちらつく、中盤練習の終局判定(空き≤24で即座に結果画面へ遷移)が唐突、`allMoves`空配列時にオーバーレイが無条件で消え真の終局か過渡状態か区別できない → 「終局近くで消える」の主因。

T054(Worker共有化)とT055(終局・パス判定の統一、想定)は同じファイル群を触るため直列実行。T054を先に着手。
- **T054完了(2026-07-10)**。`app/src/engine/sharedClient.ts`新設(モジュール内キャッシュでEngineClientを1インスタンスのみ生成)、対局・定石練習・中盤練習・詰めオセロ・棋譜解析に加え言語化トレーニング(スコープ外だが同一問題を持つため拡張)の計9ファイルで、モードごとの独自Worker生成/破棄をやめ共有インスタンスを使うよう変更。実機確認でモード12回切替+実際にエンジン利用してもpattern_v2.binリクエストは初回1回のみを確認(本番URLでも再現)。verifier・reviewerとも合格。reviewerの非ブロッキング指摘: (1) Worker終了廃止によりアンマウント後も進行中リクエストが解決するようになり、破棄済みコンポーネントのsetStateが呼ばれうる(実害は未確認、各モードが独立クロージャのため混線は無いと判断)、(2) 「モードコンポーネントが本当にgetSharedEngineClient経由になっているか」を検査する自動テストが無く目視確認頼り。
- **T055完了(2026-07-10)、評価値表示の不安定さ対応(T054・T055)完結**。中盤練習・詰めオセロのパス処理を、対局モード(gameLoop.ts)・棋譜解析(branchTree.ts)と同じ「着手適用と同期した手番解決」パターンに統一(`resolveNextSideOrFallback`新設)、旧非同期useEffectを削除。中盤練習の「空き24以下で即座に結果画面遷移」はT021作業ログ確認の結果、意図的な設計上の簡略化と判明(バグではない、reviewerもshould扱いで容認済み)。閾値は変更せず、完全読み確定中に「終盤の完全読みで結果を確定しています…」という`finalizing`表示を追加し唐突さを緩和。実機確認(自動プレイ、中盤練習13手/詰めオセロ14問)でちらつきシグネチャ0件、詰めオセロでは実際のパス発生を直接検出しちらつきなしを確認。verifier・reviewerとも合格。reviewerの非ブロッキング指摘: (1) 詰めオセロの新規テストがコンポーネント非依存でresolveMoverを再検証するのみ、(2) resetSessionToの新規checkEnd呼び出しがfire-and-forgetでキャンセルガードが無い(T054指摘と同種の既知の懸念)、(3) 検証用一時スクリプトの記載と実体の食い違い(実害なし)。

## タスク一覧(ユーザー緊急報告 2026-07-11: 評価値スケール異常+キャッシュ問題)

ユーザー報告: 棋譜解析の終盤評価値が石数差の理論上限(±64)を超える異常値(+105等)になる、グラフが定石外れ後に急落したまま高原状態になる。explorer調査で根本原因判明: T045パターン評価(pattern_v2)の生出力にクランプが無く、終盤の一部合法手が共有タイマーでタイムアウトしヒューリスティックにフォールバックした際、学習データの薄い局面で±64を超える値を出す。さらにanalyzeGame.tsのevalSource判定がbestの評価方式のみを見ており、実際に打たれた手(played)がフォールバックしていても「終局(完全読み確定)」と誤表示するバグも判明。T056の累積方式に一度混入すると恒久的に残るため急落したまま戻らない見た目になっていた。ユーザー指示: 中盤戦も含めスケールを一貫させること、Edaxのレファレンス実装を再確認すること。

追加でユーザー報告: 棋譜解析結果のキャッシュ(IndexedDB)がエンジンバージョンを考慮せず、デプロイ後も古い結果を返し続ける問題。手動クリア機能も要望。

- **T059完了(2026-07-11)**。Edaxの実装(`src/const.h`のSCORE_MIN/MAX=±64、`src/midgame.c`の`search_eval_0`でのクランプ)を実際に取得して確認し、同じ設計を採用。`engine/src/search.rs`の`static_eval`(中盤ヒューリスティック・終盤タイムアウトフォールバックの唯一の呼び出し経路)に±6400centi-disc(±64石)のクランプを追加。`app/src/analysis/analyzeGame.ts`のevalSource/isExact判定を`best`だけでなく`played`側の評価方式も見るよう修正(タイムアウトフォールバックした手を「完全読み確定」と誤表示しないように)。FFO終盤テスト全問正解・ノード数不変(終盤ソルバー無影響を確認)。自己対戦12局中3局で旧バグ(centi-disc 6588〜8876=石差+65.9〜+88.8という不可能な値)を実際に再現でき、修正後は妥当な値になることを確認。Edax比較でクランプが理論上限ちょうどで発動した実例も確認。verifier・reviewerとも合格(verifierが実際のWTHOR実戦棋譜60手で異常値0件・evalSource切り替わりの自然さを本番URLで再現確認)。reviewerの非ブロッキング指摘: (1) 終盤タイムアウトフォールバック分岐を直接叩く統合テストが無い、(2) クランプ済み同点局面でのムーブオーダリングのタイブレーク挙動への影響(実害小)、(3) **T056累積評価モデルは理論上まだ±64を超えうる残存リスクがある(合成的な敵対的テストでのみ再現、実際のWTHOR対局では未再現)、フォローアップタスク化を推奨**、(4) Edax比較セットにパターン評価のオーバーフロー実例が無い。

T059(engine側クランプ+analyzeGame.ts)とT060(cache.ts+UI)は別ファイル群のため並行実施。
- **T060完了(2026-07-11)**。`app/src/analysis/cache.ts`に`ANALYSIS_ENGINE_VERSION`定数(手動インクリメント運用)を追加、キャッシュキーにバージョンサフィックスを付与。「解析キャッシュをクリア」ボタンをAnalysisMode.tsxに追加。verifier・reviewerとも合格。verifierが実際に旧形式キャッシュへ異常値を仕込んで再解析させ、新形式キーで正しく再計算されエンジンが再実行されることを動作確認済み(古いキャッシュが実際にヒットしなくなることを実証)。reviewerの重要な指摘: T059(評価結果が変わる変更)は本タスクのANALYSIS_ENGINE_VERSIONを必ずインクリメントする必要があるとして、T059実装エージェントに追加指示済み。verifierが検証中にT059側で既にversion 2への更新(クランプ修正の説明コメント付き)が行われていることを確認(連携成功)。reviewerの非ブロッキング指摘: 手動インクリメント運用に機械的な強制力(CI等)が無く将来の失念リスクが構造的に残る(フォローアップ候補として記録)。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T059 | 評価値スケール異常の修正(パターン評価のクランプ欠如+終局完全読みの誤表示バグ) | implementer | done | 0 |
| T060 | 棋譜解析結果キャッシュにエンジンバージョン管理+手動クリア機能を追加 | implementer | done | 0 |

## タスク一覧(ユーザー追加依頼 2026-07-11: 棋譜解析の評価値モデルを累積ロス方式に再設計)

ユーザー指摘: 棋譜解析の評価値の考え方が根本的に誤っている。各手ごとに独立したヒューリスティック探索値をそのまま表示しているため、最善手を打っただけなのに評価値が大きく動いて見える(スクリーンショットで「◎最善」なのに「+18」「逆転」と表示される不整合を提示)。正しくは「最善手が続く限り評価値は前の値を維持し、悪手のときだけそのロス分だけ悪化する」という累積ロス方式に作り直すべき、との指示。数学的に検証済み(telescoping性質: E[i]=E[i-1]∓loss[i]で、最終的に実際の最終石差に収束する)。既存のロス計算(同一局面での最善手と実際の手の比較)自体は流用し、表示層の再構成のみで実現できる設計。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T056 | 棋譜解析の評価値モデルを「累積ロス方式」に作り直す(最善手は評価値維持、悪手のみ悪化) | implementer | done | 0 |

- 2026-07-11 ユーザー追加依頼(優先度低・「後からでよい」と明示): 棋譜解析・中盤練習の悪手分析パネルの言語化説明(T031特徴量分解・T032モチーフ検出・T033反証層)が読みにくいので、盤面も使ってわかりやすく説明してほしい。T056(BlunderPanel.tsx周辺と重なりうる)完了後に着手予定。次のタスク化はT056完了後に行う。
- **T056完了(2026-07-11)**。`applyCumulativeEvaluation`関数を新設し、評価値をE[0]=0起点の累積ロス方式(黒番は減算・白番は加算)に全面刷新。終局側から最終局面を先読みして逆算する旧2パス構成を廃止し、前進累積のシンプルな1パスに置き換え(副次効果として不要な追加エンジン呼び出しも削減)。ユーザー報告シナリオ(酉定石13手+定石外の最善手e6)で実機再現し、最善手で評価値+0・ロス±0・逆転なしを確認(旧バグ解消)。verifier・reviewerとも合格。**申し送り事項(ユーザー確認推奨)**: 「逆転」判定がMath.sign比較のため、E[0]=0からの最初の悪手は(ロスの大小によらず)必ず「逆転」表示になる仕様。ユーザーの原意図(「本当に形勢が入れ替わった場合のみ逆転」)とは若干ズレる可能性があり、代替実装(0を特別扱いしない厳密な符号反転判定、1行で差し替え可能)も判明している。verifierの申し送り: telescoping性質の新規テストは合成ロス値による恒等式検証に留まり、実エンジンのヒューリスティック評価を使った完全なend-to-end収束の実機証明はコスト上未実施(受け入れ基準外、設計上の理論的留保として記録)。
- ユーザー確認の結果、「厄密な符号反転のみを逆転とする」修正を選択(T057)。
- 2026-07-11 ユーザー追加依頼(規模大・「機能面が整ってきたら」と条件付き): モードによって盤面サイズが違う等UI全体が洗練されていない。機能面が整ったらUIを洗練させ、タイトル画面も作り、エフェクト・細部にこだわり、厳しく評価して遊んでいて楽しいアプリにしてほしい(設計書フェーズ7「磨き込み」に相当)。T059完了後、まずUI/UX監査(explorer)を行い、優先順位を含めた計画をユーザーに確認してから着手する方針。
- UI/UX監査完了。優先度高い8項目を特定: (1)盤面サイズ統一 (2)デザイントークン一元化 (3)タイトル/ホーム画面 (4)石の反転アニメーション (5)勝敗演出リッチ化 (6)ボタン等のホバー/クリックフィードバック (7)アクセントカラー統一(紫/青/緑混在) (8)アイコン/マニフェストの作り込み。ユーザーは「基盤(1〜2)から順に1つずつ」を選択。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T061 | UI磨き込み(1): 盤面サイズの統一+デザイントークン基盤の導入 | implementer | done | 1 |
| T065 | UI磨き込み(3): タイトル/ホーム画面の新規追加 | implementer | done | 1 |
| T066 | UI磨き込み(4): 石の反転アニメーション | implementer | done | 1 |
| T067 | UI磨き込み(5): 勝敗演出のリッチ化 | implementer | done | 0 |
| T068 | ダークモードでの可読性修正(OS設定連動) | implementer | done | 1 |
| T069 | UI磨き込み(6): ボタン等のホバー/クリックフィードバックの統一 | implementer | done | 0 |
| T070 | UI磨き込み(7): アクセントカラーの統一(「現在選択中」表示の紫統一) | implementer | done | 0 |
| T071 | UI磨き込み(8、最終項目): アイコン/マニフェストの作り込み | implementer | done | 0 |

- **T068完了(2026-07-11、1回のやり直し後)**。`index.css`に背景色・文字色系のデザイントークン(`--color-bg*`/`--color-text*`/`--color-border`等)を追加し、`@media (prefers-color-scheme: dark)`で暗色側の値を定義。「固定ライト背景+文字色`inherit`/未指定」パターン(モード切替タブ・悪手分析パネル・用語集ポップアップ・タイトル画面カード等)を横断的にトークン参照へ置き換え。やり直し理由: reviewerが`.notice`/`.notice--error`(エラー・警告メッセージ、40箇所超で使用)のダーク対応漏れを発見(コントラスト比2.7:1/3.5:1でAA基準未達、ユーザー報告の症状に直結)。修正の過程で、実は`.notice--error`と`.notice`が同じCSS詳細度でソース順により`.notice`が常に勝ち、エラー色が元々一度も画面に反映されていなかったという既存バグも発見し、`.notice.notice--error`複合セレクタへの変更で合わせて解消(verifier・reviewerとも独立に妥当性を確認)。再検証でWCAG AA基準(4.5:1以上)を満たすことを確認、本番URLでも実機確認済み。盤面Canvas配色は仕様通り無変更。
- 2026-07-11 ユーザー追加報告: 「ダークモードでまともに読めなかったりする」。explorer調査により`prefers-color-scheme`対応が皆無(`index.css`の`color-scheme: light dark`宣言のみで実際のダーク配色トークン無し)、複数箇所で「固定ライト背景+文字色`inherit`/未指定」の組み合わせによりダーク環境で読めなくなることが判明。T068として起票。T067が`app.css`を編集中のため、ファイル競合を避けてT067完了後に着手する(直列実行)。

- **T061完了(2026-07-11、1回のやり直し後)**。デザイントークン(盤面サイズ・アクセントカラー・角丸・余白)を`index.css`に導入。やり直し理由: 初回実装はCSS変数の参照自体は正しかったが、練習系3モード(定石練習・中盤練習・詰めオセロ)の盤面が親flexコンテナの`align-items:center`により伸長されず、canvas既定の300pxにフォールバックしていた(対局モードの640pxと不一致、ユーザー指摘の核心的な問題が未解消のまま合格していた)。修正: `.board-container`に`width:100%`を1行追加。再検証で全モード640px(375px幅では359px)で統一されていることを実測確認(mdサイズ320px・smサイズ260pxの参考盤面も実データで到達し確認)。verifier・reviewerとも合格。reviewerの非ブロッキング指摘: `verbalize/PracticeMode.css`の`.verbalize-problem`がまだハードコード640pxのまま(スコープ外、次回統一候補)。
- **T071完了(2026-07-11)、UI/UX磨き込み優先度8項目すべて完了**。PWAアイコン(`icon-192.svg`/`icon-512.svg`)をT014時点のプレースホルダー(単純な黒丸+白破線+白丸)から、`--color-accent`グラデーション背景+盤面+オセロ開局配置の石という意匠性のあるデザインに刷新。`purpose:"maskable"`バリアント(中心80%セーフゾーン内に収まるよう座標計算)を新設し`manifest.json`に追加。iOS非対応だった`apple-touch-icon`(SVG)をPlaywrightでレンダリングした実PNG(180x180)に置き換え。`favicon.svg`もアプリアイコンと統一されたデザイン・配色に刷新。verifier・reviewerとも合格(PNGマジックナンバー・セーフゾーン座標計算・本番デプロイまで独立検証)。reviewerの非ブロッキング指摘: グラデーション明側の色が`--color-accent`(#863bff)ちょうどではなくやや明るい`#9c5cff`(同系トーン、実害なし)。**これによりUI/UX監査で洗い出した8項目(盤面サイズ統一・デザイントークン一元化・タイトル/ホーム画面・石の反転アニメーション・勝敗演出のリッチ化・ボタン等のホバー/クリックフィードバック・アクセントカラー統一・アイコン/マニフェストの作り込み)がすべて完了。**
- **T070完了(2026-07-11)**。棋譜解析モードのムーブリスト「現在選択中の行」・悪手分析パネルの分岐ツリー「現在地」ノード・評価グラフのホバー線/ホバー点/現在手ポイント・評価内訳ラベルのホバー色に紛れ込んでいた青系の色(`#e0e7ff`/`#312e81`/`#6366f1`/`#2563eb`)を、T061のアクセント紫トークン(`--color-accent`系)に統一。固定背景バッジ用途と可変背景上の文字色用途でトークンを使い分け。正誤バッジ・勝敗演出・評価ソース色分け・盤面凡例等の「意味を持つ色」は意図通り無変更。verifier・reviewerとも合格、ダークモード(T068)との整合性も確認済み。
- **T069完了(2026-07-11、1回のエージェント無応答からの引き継ぎあり)**。全`<button>`要素にグローバルな`:hover`(`filter: brightness(0.96)`)・`:active`(`filter: brightness(0.9)`+`transform: scale(0.97)`)・`:focus-visible`(`outline: 2px solid var(--color-accent)`)を要素セレクタで追加し、既存の個別ボタンスタイル(`.title-screen__card`等)とCSS詳細度により共存する設計。`.blunder-panel__why-bad-item--hoverable`の`outline: none`によるキーボードフォーカス不可視問題も修正。`prefers-reduced-motion: reduce`で`transform`を抑制。運用上の出来事: 最初のエージェントが実装まで進めた時点で約46分無応答となり、生存確認(mtime停滞+SendMessage無反応)で非稼働と判断、別エージェントに引き継いだ(実装自体は完成しておりテスト・ビルド・実機確認・デプロイのみで完了)。verifier・reviewerとも合格(実マウス押下・実Tabキー操作による直接検証)。reviewerの非ブロッキング指摘: `.title-screen__card`はクラスセレクタの`transition`がグローバル`button`の`transition`ショートハンドを上書きするため、そのカードのみ`filter`変化がアニメーションせず瞬時切り替えになる(実害は軽微)。
- **T067完了(2026-07-11)**。対局モードの終局時、装飾の無いテキスト表示を、勝ち(紙吹雪12個+弾む登場)/負け(静かなフェードインのみ)/引き分け(`--color-accent-bg`/`-dark`トークンの中立色)に出し分ける`ResultCelebration`コンポーネントを新設。T066の`FLIP_ANIMATION_MS`(220ms)経過後に表示するタイミング制御で石反転アニメーションと重ならないようにした。`prefers-reduced-motion: reduce`で紙吹雪・アニメーションを抑制。verifier・reviewerとも合格、T066の教訓を踏まえ`getComputedStyle`のアニメーション名・紙吹雪DOM要素の実在確認等、直接的な手法で演出の発火を確認(dipRatioのような間接指標は不使用)。
- **T066完了(2026-07-11、1回のやり直し後)**。`Board.tsx`(全モード共通のCanvas盤面コンポーネント)に石の反転・出現アニメーションを追加。新旧盤面から「1手適用による通常遷移か」を判定する純粋関数`boardDiff.ts`(石数+1判定)を新設・単体テスト化。やり直し理由: 初回実装は`ResizeObserver.observe()`の仕様上保証される初回コールバックが`hasDrawnOnce`フラグと衝突し、着手のたびにアニメーションが着手後数十ms(2フレーム程度)で強制キャンセルされ実質見えなくなっていた(verifier・reviewer双方がCanvas描画API(`scale`)の実引数を直接記録する手法で独立に発見。implementerの自己申告=キャンバス塗り面積比較(dipRatio)は誤検知だった)。修正: `useEffect`を「キャンバスサイズ追従用(マウント時1回のみResizeObserver生成)」と「描画・アニメーション用」の2つに分離し根本原因を除去。再検証でも同じ直接的手法(scale実引数の記録)でアニメーション中の中間フレーム(潰れた状態のスケール値)が実際に発生していることを確認。**教訓: アニメーション等の視覚効果の検証では、間接指標(キャンバスの塗り面積比較等)だけでは偽陽性を検出できないことがあるため、Canvas描画APIの実引数を直接記録する等のより直接的な検証手法を優先すること。**
- **T065完了(2026-07-11、1回のエージェント無応答からの引き継ぎあり)**。アプリ起動時にまずタイトル/ホーム画面(アプリ名・キャッチコピー・6モードへの説明付きカード)を表示するよう変更(`app.tsx`の`mode`state初期値を`null`化)、`mode-nav`にホームボタンを追加してモード画面からいつでも戻れるようにした。新規コンポーネント`TitleScreen.tsx`/`.css`はT061のデザイントークンを再利用。**運用上の出来事**: 最初に着手したエージェントが実装(4ファイル分の未コミット変更)まで進めた時点で30分以上無応答となり、オーケストレーターの生存確認(ファイルmtime停滞+SendMessage無反応)により非稼働と判断、別エージェントに「既存差分を評価の上、活かせるものは活かして完成させる」形で引き継いだ(書き直しではなく完成のみで済んだ)。verifier・reviewerとも合格(verifierはimplementerの自己申告=対局モードのみの確認だったのに対し、6モード全てのカード遷移を個別に独立再現)。reviewerの非ブロッキング指摘: ホームボタンの`aria-label`が可視テキスト「ホーム」と異なる文言になっている(軽微、次回ポリッシュ候補)。

**【最重要・緊急】** ユーザーが本番URLでT059修正後も異常値を確認、調査の結果、Service Workerのfetchハンドラがページ本体(index.html)までcache-first戦略にしており、かつ更新検知・リロード促進の仕組みが皆無なことが判明。通常ユーザーはデプロイ後も手動キャッシュクリアなしでは新バージョンを見られない。**これまでの本番確認はすべて「手動でキャッシュクリアしてから確認」という手順を踏んでおり、この問題自体を検知できていなかった(検証プロセスの重大な抜け穴)**。T062で緊急修正。T061(CSS)とはファイルが異なるため並行実施。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T062 | Service Workerの更新伝播を修正(デプロイ後も古いアプリ本体が表示され続ける不具合) | implementer | done | 1 |
| T063 | 棋譜解析の評価グラフに軸の数値表示とカーソル追従ツールチップを追加 | implementer | done | 0 |
| T064 | 累積評価値(T056)を理論上限±64にクランプする(悪手連発時の発散バグ修正) | implementer | done | 0 |

- 2026-07-11 ユーザー追加報告: 「評価エンジンのスケールは良さそうだが、グラフがちゃんと表示できていない気がする、グラフが使いにくい」。T063の検証時に、新機能確認だけでなく「グラフ表示自体の不具合の有無」も重点的に確認する方針。
- **T064完了(2026-07-11)**。`applyCumulativeEvaluation`の`after`算出箇所に理論上限`±64`(`DISC_DIFF_THEORETICAL_MAX`)のクランプを追加。`before`は常に前手のクランプ済み`after`を引き継ぐ設計のため、この1箇所のクランプで全局面が範囲内に収まる。T057の厳密な逆転判定(クランプは符号を変えない)・telescoping性質(通常対局では非発火)は維持。新規テスト(24手の一方的な悪手連発シナリオ、`resolveMover`による実データ生成)で最終値が実際に`-64`で頭打ちになることを検証。verifier・reviewerとも合格、dev・本番(`https://giwarb.github.io/othello-trainer/`)双方で30手の悪手連発棋譜を解析し、評価列・グラフとも±64超過なしを確認。
- **T063完了(2026-07-11、T064待ちのblockedから復帰)**。縦軸に自動スケール数値目盛り(`axisConfigFor`、±8〜±64の4段階)、SVGネイティブ`<title>`に代わるPointer Events駆動のカーソル追従カスタムツールチップ(手数/手番/着手/評価値/ロス/分類)を実装。当初verifierが「グラフではなく入力データ(T056累積評価)が±64を超えて発散する」バグを発見しT064を分離起票、blockedとしていたが、T064完了後の再検証で`EvalGraph.tsx`自体は無修正のまま全受け入れ基準を満たすことを確認(合格)。
- **T062完了(2026-07-11、1回のやり直し後)**。fetchハンドラをナビゲーション用(ネットワーク優先)と静的アセット用(cache-first維持)に分離、更新検知バナー(自動リロードなし、「今すぐ更新」クリック時のみリロード、無限ループ構造的に不可能)を追加。やり直し理由: 初回実装はService WorkerのCache Storageは正しくバイパスしたが、**ブラウザのHTTPキャッシュ層(別物)をバイパスしておらず**、GitHub Pagesの`Cache-Control: max-age=600`により最大10分間ネットワーク優先が骨抜きになりうる不具合が発覚(reviewerが実際のレスポンスヘッダから発見)。修正: `fetch(request, { cache: 'no-store' })`に変更、エラーレスポンス時のキャッシュフォールバックも追加。verifierが本番URLで実際にタブを閉じて再訪問するシナリオを再現し、Chrome DevTools Protocolで`fromDiskCache: false`(HTTPキャッシュを経由せず実際にネットワークに到達)を確認、reviewerも最終コミットを確認し合格。**この一連の不具合は、これまでの本番確認手順(手動キャッシュクリア)自体が問題を隠蔽していたことが根本原因であり、今後は「キャッシュ事前クリアなしの素の再訪問」を検証標準に含める。**
- **T057完了(2026-07-11)**。`reversal`判定を`Math.sign`比較から`(before>0 && after<0) || (before<0 && after>0)`(0を特別扱いしない厳密な符号反転)に変更。verifier・reviewerとも合格、本番URLで20手の実在合法棋譜を使い全19手の逆転判定が新基準と一致することを確認(0からの遷移は逆転扱いされない、実際の符号反転のみ逆転)。T056で発見された「E[0]=0からの最初の悪手が常に逆転扱い」問題を解消。verifierより運用上の注意: 並行実装中の他タスク(T058)の未コミット変更がメイン作業ツリーの`npm run build`検証を汚染しうるため、git worktreeで独立検証した(今後も並行タスクがある場合は同様の配慮が必要)。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T057 | 「逆転」判定を厳密な符号反転(+から-、またはその逆)のみに限定する | implementer | done | 0 |
| T058 | 棋譜解析の悪手分析パネルを、盤面と説明が連動する分かりやすいレイアウトに再設計 | implementer | done | 0 |

調査(explorer)により、悪手分析パネル(BlunderPanel.tsx)は盤面と文章説明が空間的に分離しており、確定石数の計算がwhyBad.tsとAttributionWaterfall(engine::eval::stable_count)で二重実装され値が食い違いうるバグリスクも判明。T058でこれらを解消。中盤練習モードへの同等拡張はスコープ外とし将来タスク化。
- **T058完了(2026-07-11)**。確定石数の二重実装をengine/src/eval.rsと同一アルゴリズムに統一(バグ修正)。評価内訳・モチーフ・なぜ悪いかの各項目にホバー/フォーカスで盤面ハイライトが連動する配線を追加、反証層はタブ切替から実際の進行/最善進行の常時2列表示に再設計、評価内訳ラベルから用語集起動も可能に。verifier・reviewerとも合格。reviewerの非ブロッキング指摘: (1) 評価内訳のハイライトが着手前局面への近似であることがUI上で開示されていない(数値自体は食い違わないが誤解を招きうる)、(2) motifHighlightSquares(15分岐)に単体テストが無い、(3) 検証用一時ファイルの残存(実害なし、後日クリーンアップ済み確認)。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T055 | 終局・パス判定の非同期タイミングを統一し「評価値が終局近くで消える」問題を解消 | implementer | done | 0 |

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T054 | エンジンWorkerをモード切替をまたいで共有し「評価値が最初に出ない」問題を解消 | implementer | done | 0 |

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T053 | エンジン強化(ETC・終盤パリティ)をWASM再ビルドで本番反映 | implementer | done | 0 |

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T052 | エンジン強化(3): 終盤完全読みへのパリティベース着手順序付け導入 | implementer | done | 0 |

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T051 | エンジン強化(2): ETC(Enhanced Transposition Cutoff)による探索高速化 | implementer | done | 0 |

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T048 | エンジン強化(1): MPC(Multi-ProbCut)による中盤探索の枝刈り高速化 | implementer | done | 0 |

- **T050完了(2026-07-10)**。定石DBを90→112ラインに拡充。独立した2ソース(uguisu.skr.jp、note.com/mjulinのオセロクエスト戦法一覧)と照合し、鼠定石系統が完全未収録だったこと等22ラインの拜落を発見、bassy84.netの実ページをcurl+grepでverbatim照合してから追加(T016教訓の徹底)。DAG再構築(507→615ノード)。verifier・reviewerとも合格、**verifierが22ライン全件を独立に再curlし文字列照合、捏造・誤記載なしを確認**。「たまぷらーざ」はサイトマップのリンク文言と実ページ内容(フラットコンポス)が不一致のため追加見送り(見送り理由・見送りリストも作業ログに正直に記録)。「ロケット定石」は既存「闘牛C」と着手列完全一致のためalias統合。reviewerの非ブロッキング指摘: 作業ログの「鶴定石(15手)」表記が実データ(16手)と1手ずれ(実データ自体は正しい)。
- **T049完了(2026-07-10)**。評価値・ロス表示を全モードで整数(石単位)表示に統一(`Math.round`ベース)。タスク仕様が明示した3ファイルに加え、同スケールの表示箇所(app.tsx・joseki/PracticeMode.tsx・midgame/EvalBar.tsx等)へスコープを拡張(要件1の趣旨に基づく妥当な判断とverifier・reviewerとも確認)。内部計算・悪手判定閾値には影響なし。verifier・reviewerとも合格、本番URLで実機確認済み。reviewerの非ブロッキング指摘: 微小負値(-0.4等)が丸め後`+0`表示になる点(JS仕様上の`-0`挙動、UIとしては許容範囲)。

ユーザー追加依頼(2026-07-10): (1) 評価値表示を石数差らしい整数表示にしてほしい(Edaxは中盤評価も整数っぽく出る)。(2) 定石DBの有名どころ(虎・兎・牛・ローズビル・酉等)をしっかり押さえてほしい。T048(engine/src)とは無関係なファイルのため並列実施。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T049 | 評価値表示を石数差らしい整数表示に変更(Edax準拠) | implementer | done | 0 |
| T050 | 定石DBを有名定石一覧と照合し拜落を洗い出して強化 | implementer | done | 0 |

## タスク一覧(ユーザー追加依頼 2026-07-11: 中盤練習の判定・悪手説明強化+盤面座標表示)

ユーザーより4件: (1)中盤練習のデフォルト判定モードを「厳格」に (2)中盤練習の失敗時に最善手との差をすぐ分かりやすく表示 (3)モチーフパターンをクリックすると盤面上に該当箇所をハイライト (4)全モードの盤面に座標記号(a-h/1-8)を常時表示。(1)〜(3)はいずれも`midgame/PracticeMode.tsx`を触るためT072に統合。(4)は`components/Board.tsx`のみを触る独立タスクのためT073として並行実施。

- **T072完了(2026-07-11)**。中盤練習の判定モード(`JudgeMode`)デフォルトを`'standard'`から`'strict'`(最善手のみ正解)に変更、あわせて`localStorage`永続化コードが存在せずセッションをまたぐたびに設定が失われる既存バグも修正。失敗時の説明UIを、棋譜解析の悪手分析パネル(T058)と同じロジック(`whyBad.ts`/`motifs.ts`/`attribution.ts`/`refutation.ts`/`BoardOverlay.tsx`、いずれもimportで再利用しロジック複製なし)を使って評価内訳waterfall・モチーフ検出タグ・反証層・「なぜ悪いか」表示に拡張。モチーフタグはクリックで盤面ハイライトがON/OFF切り替わる(棋譜解析側の既存のホバー連動・クリック=用語集起動という挙動は意図的に無変更)。verifier・reviewerとも合格、独立検証で棋譡解析モード側への回帰も無いことを確認。
- **T073完了(2026-07-11)**。`components/Board.tsx`のCanvas描画に列(a-h)・行(1-8)の座標ラベルを追加、全6モードに自動反映。実装中に「石の縁でラベル文字が読めなくなる」不具合を自己発見し、最前面描画+アウトライン文字(縁取り)で解消。verifier・reviewerとも合格、クリック→着手結果のピクセルサンプリングでラベルと内部座標表現の対応関係を独立検証済み、本番URLでも確認済み。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T072 | 中盤練習の判定デフォルトを厳格化+失敗時の説明UIを盤面連動・モチーフクリック対応に拡張 | implementer | done | 0 |
| T073 | 盤面に列(a-h)・行(1-8)の座標ラベルを常時表示(全モード共通) | implementer | done | 0 |

## タスク一覧(ユーザー追加依頼 2026-07-12: 言語化トレーニング非表示+定石練習ゲームオーバー画面強化)

ユーザーより2件: (1)言語化トレーニングをナビゲーションから非表示に(コードは残す、将来復活を想定)(2)定石練習のゲームオーバー画面に、自分の進行と、そこまで一致していた定石名を並記表示。(1)は`app.tsx`のみ、(2)は`joseki/PracticeMode.tsx`のみを触るため並行実施。

- **T074完了(2026-07-12)**。`app.tsx`に`NAV_VISIBLE_MODES`(`verbalize`を除外したモードキー一覧)を新設し、タイトル画面カード・`mode-nav`タブ生成の両方をこれに置き換え。`VerbalizeMode`本体・`app/src/verbalize/`配下・IndexedDBスキーマは無変更(将来の復活を想定)。verifier・reviewerとも合格。
- **T075完了(2026-07-12)**。定石練習のゲームオーバー画面に、定石外の一手を含む全体棋譜(最後の一手は波線下線で強調)と、そこまで一致していた定石ライン名のフラット列挙(`lookupJosekiNode(...).names`を再利用、新規API不要)を追加。T026(クリア画面)の「主役+補足」構造は「唯一の到達ライン」という前提が成立しないゲームオーバーには適用できないため、意図的にフラット列挙という別設計を採用。verifier・reviewerとも合格。**興味深い副次的発見**: 「一致ラインが0件」の表示分岐は要件通り実装されているが、定石DBの構築ロジック(全ノードに必ず1つ以上のライン名が伝播する不変条件)と黒の初手正規化(`opForFirstMove`)により、現行データでは実プレイ上到達不能な防御的分岐であることをimplementer・reviewer・verifier全員が独立にコード検証で確認(バグではなく、将来のDB変更等に備えた安全な設計判断)。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T074 | 言語化トレーニングモードをナビゲーションから非表示にする | implementer | done | 0 |
| T075 | 定石練習のゲームオーバー画面に自分の進行と一致していた定石名を表示 | implementer | done | 0 |
| T076 | 中盤練習の複数候補手評価で時間予算共有により最善手判定が不正確になる問題の修正 | implementer | done | 0 |

- 2026-07-12 ユーザー報告(スクリーンショット付き): 中盤練習モードのある局面で、実際に打った手(b4)が「失敗」、b2(隅隣接のX打ち)が「正解手」と判定され、ユーザーが評価関数のバグを疑った。explorerが実際の局面をビットボード化しEdax(`bench/edax-compare/edax-extract/`に導入済み)・本エンジン双方で深い探索により検証した結果、**静的評価関数にバグは無く**、深く読めばb4の方がb2よりずっと良いとEdax・本エンジンとも一致(ユーザーの直感どおり)。しかし**アプリが実際に使う設定(`MIDGAME_ANALYZE_LIMIT`, depth16/time300ms)では、300msの時間予算が合法手数(本局面12箇所)に累積で共有される設計(T034由来の意図的なハング防止策)のため、1手あたりの実効探索が浅くなり誤判定していた**ことが判明。T076として起票。
- **T076完了(2026-07-12)**。`engine/src/search.rs`の`search_all_moves_with_eval`に、残り時間予算を残り候補手数で公平に分配する`fair_share_time_ms`関数を新設(早く完走した手の余り時間は自動的に後続の手に回る)。従来の「早い者勝ち」配分(たまたま先頭の1手が予算をほぼ独占し残り全ての手がdepth=1のノイズ同然の評価になる)という設計不良を根本修正。`time_ms`未指定の既存呼び出し(FFO・詰めオセロ・対局モード等)は無変更。あわせて中盤練習・言語化トレーニング関連4箇所の`timeMs`を300→1000msに引き上げ(根本修正を前提とした追加チューニング)。verifier・reviewerとも合格、報告局面を独立に再現しEdax(黒視点b2=-1石、b4=+14石)と本エンジン双方で修正後の判定が一致することを確認、T034のハング防止(26手棋譜解析、約39秒)にも回帰が無いことを確認。

## タスク一覧(ユーザー追加依頼 2026-07-12: 対局モード拡張(2人対戦・評価値表示・盤面自由配置))

ユーザーより3件: (1)対局モードで人間vs人間(CPU無し)で打てるように (2)現在の評価値表示を切り替え可能に (3)盤面を自由配置してそこから開始(分析にも使いたい、との要望あり)。(1)(2)(3)はいずれも`app.tsx`/`gameLoop.ts`を触るため1タスク(T077)に統合。棋譜解析モードへの盤面自由配置の統合(要望(3)の後半)は、T077で新設する`BoardEditor`コンポーネントが安定してから別タスクとして続けて着手する。

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T077 | 対局モードに人間vs人間モード・評価値表示切替・盤面自由配置からの開始を追加 | implementer | done | 0 |
| T078 | 中盤練習の候補手オーバーレイ表示と悪手判定が別々の探索呼び出しで不整合を起こす問題の修正 | implementer | done | 0 |

- 2026-07-12 ユーザー報告(スクリーンショット2枚): 中盤練習モードで、候補手評価オーバーレイが「g3=±0(最良)」と表示していたのに、実際にg3を打つと「失敗、正解手はd7」と矛盾する判定が出た。explorerが調査した結果、評価関数のバグではなく、**オーバーレイ表示用と着手後判定用で別々に(2回)エンジンを呼び出しており、探索が壁時計依存(T076で導入)のためこの2回の結果がわずかに揺らぎうる**ことが原因と判明(T076の作業ログにも既知の性質として記録済み)。「厳格」判定モード(僅差でも即失敗)と組み合わさり、ユーザー体験を損なう実害があるため、T078として起票(オーバーレイ取得済みの評価結果を判定にも再利用し、2回の独立呼び出し自体を無くす設計)。T077(対局モード、app.tsx/gameLoop.ts)とは触るファイルが異なるため並行実施。
- **T078完了(2026-07-12)**。`getAnalyzedMoves(board, side)`(着手前局面の参照同一性+手番でキャッシュ)を新設し、候補手オーバーレイ用のeffectと`handlePlayerMove`の判定処理の両方をこの1回だけのエンジン呼び出しに統一(オーバーレイON/OFFに関わらず内部的に1回のみ取得)。verifierが「表示上の丸めでなく生データの数学的な最善手」を実際にクリックする厳密な検証(ローカル9試行・本番9試行)で、判定不一致0件・重複エンジン呼び出し0件を確認。reviewerの非ブロッキング指摘: エンジン呼び出し失敗時にrejectされたPromiseがそのままキャッシュされ続け、その局面で永続的に判定不能になりうる(`joseki/lookup.ts`の`loadJosekiDb`の失敗時リセットパターンを参考に、次回タスクでの改善を推奨)。**なお、本番検証中に発見された「評価の優勢/劣勢が入れ替わりました」という別の失敗理由は、本タスクの対象外である`checkEnd`(終局時の完全読みによる最終石差判定)由来であり、T078が修正した着手ごとの判定ロジックとは無関係であることをverifierが確認済み。**

| T079 | 棋譜解析モードに盤面自由配置エディタを統合し、任意局面から解析できるようにする | implementer | done | 0 |

## タスク一覧(リポジトリ整理 2026-07-13 ユーザー依頼)

| ID | タスク | 担当 | 状態 | 試行 |
|---|---|---|---|---|
| T080 | リポジトリ整理(README全面書き換え・.gitignore整理・デバッグ残骸削除) | implementer | done | 0 |
| T081 | Codex(gpt-5.6-sol)を設計・最終レビュー担当として使うラッパースクリプト | implementer | done | 1 |

- T081 redo #1(2026-07-13): verifier 合格後、オーケストレーターのドッグフーディング(T081自身への codex-review 適用)で実バグ検出 — プロンプトを引数渡ししているため、`"` を含むタスクファイル(作業ログにgitコマンド例がある実運用のファイルほぼ全部)で PS5.1 の引数分割が発生し codex が exit 2。stdin 渡し+UTF-8 エンコード明示への修正を再委譲。verifier が検出できなかったのは受け入れ基準の Range 指定例(T080)がたまたま `"` を含まなかったため。教訓: ラッパー系のタスクは「実運用と同じ入力(汚れたデータ)」での確認を受け入れ基準に含める。
- **T081完了(2026-07-13、redo 1回)**。修正コミット `fe788a9`(stdin渡し+UTF-8化)で回帰ケース(T081自身への codex-review)が exit 0・文字化けなしを確認、Actions成功。**申し送り(codexレビューの中レベル指摘、次に scripts/ を触るタスクで対応)**: (1) `scripts/*.sh` が git 上で実行ビット無し(`100644`)のため Unix で直接実行不可(`git update-index --chmod=+x` で付与する。既存 codex-task.sh も同様か要確認)。(2) `-Range` に `--help` 等 `-` 始まりの値を渡した場合の入力検証が不十分。軽微: codex 実行中のコンソールエコーに「?」の表示乱れがあるが `-o` のレポート本体は無事(表示のみの問題)。なお `tasks/review/T081-*-codex-review.md` の「不合格」判定は修正前コミット `99dcf7a` 時点のレビューであり、指摘のブロッカーは直後の `fe788a9` で解消済み(レポート自体は経緯の記録として残す)。

- 2026-07-13 ユーザー依頼: リポジトリが散らかっているのでREADME更新・.gitignore整理・タスク状態のコミット等の整理を実施。タスク管理ファイル一式(T001〜T079の未コミット分・STATUS.md・CLAUDE.md・言語化支援設計書)はオーケストレーターが直接コミット済み(c6a9845)。README書き換え・.gitignore追記・残骸削除(err_*.log, out_*.json, single_unlabeled.txt, bench/edax-compare/*.exe)はT080としてimplementerに委譲。

- **T079完了(2026-07-12)**。T077で新設した`BoardEditor`を棋譜解析モードの新タブ「盤面を自由配置」として統合。`replayGame`/`analyzeGame`に開始局面(盤面+手番)を渡せる任意引数を後方互換な形で追加し、カスタム開始局面指定時は定石DB照会を完全に無効化(標準初期局面前提の`opForFirstMove`が投げるRangeErrorを根本回避)。verifier・reviewerとも合格、白番から開始する等の真にカスタムなシナリオで定石DB非参照(ムーブリストに「定石」表示が出ない)・悪手分析パネル・評価グラフが正しく機能することを確認、既存の「テキストで入力」「盤面で並べる」タブに回帰なし。reviewerの非ブロッキング指摘: 「盤面で並べる」と「盤面を自由配置」タブの着手積み上げロジックがほぼ重複(共通ヘルパーへの抽出は次回検討)。**これでユーザー依頼(対局モードの2人対戦・評価値表示切替・盤面自由配置、およびその棋譜解析への統合)がすべて完了。**

状態: `todo` → `in_progress` → `review` → `done` / 不合格: `redo` / 着手不能: `blocked`

## メモ・ブロッカー
- 2026-07-07 ユーザー依頼: 「GitHub Pagesで遊べる状態にして」+新設計書`othello-trainer-design-verbalization.md`をタスク化・実装。GitHubリモート`giwarb/othello-trainer`にpush可能(gh認証済み)、ローカルmainがorigin/mainより9コミット進んでいた(フェーズ1のエンジン実装。まだpushしていなかった)。
- 新設計書の概要: 中盤の言語化支援(特徴量層§1・評価内訳分解層§2・反証層§3・モチーフ検出タグ§4・可視化オーバーレイ§5・言語化トレーニングモード§6・用語集§7・概念別弱点統計§8・任意LLM解説層§9)。既存フェーズ4(棋譜解析)・6(中盤/詰め)を拡張し新フェーズ6.5を挿入。詳細はexplorerレポート参照(このセッションのコンテキストにあり)。
- 実装順序の方針: まずフェーズ2(対局UI+GitHub Pagesデプロイ)を完成させ「遊べる状態」を先に届ける。その後、言語化支援機能を着手(特徴量層・評価内訳分解層から着手し、既存のeval.rsの3項目分解から始めて段階的に拡張する想定)。
- 開発機に Rust/wasm-pack 未インストールを確認済み(Node/npm/Git/Pythonは導入済み)。T001でインストールから実施。
- T002のreviewerより: リポジトリ直下に stray `nul` ファイルと `.claude/scheduled_tasks.lock`(未追跡)が存在。ブロッキングではないが次のコミット前に削除検討。
- T003/T004のreviewerより: T002以降(bitboard/zobrist/tt/eval)が未コミットのまま蓄積していたことが判明。コミット作業をimplementerに委譲して解消済み(コミット 727eb69, 55b8545)。以後、各タスク完了時に「作業内容をコミットすること」を委譲プロンプトに明記する。
- T006 implementerより申し送り: `solve_exact(board, Black) == -solve_exact(board, White)` は両者に合法手がある局面では一般に成立しない(数学的に当然。手番が違えばゲーム木が異なるため)。今後同種のタスク仕様を書く際は「パス/終局局面に限る」旨を明記すること。
- T009完了。FFO #40-48(9問)は実行して公式正解値と一致することを確認済み。#49は実データ(2つの独立ソースで裏付け済み)を保持しつつ、計算資源制約によりオーケストレーターの指示で打ち切り、`#[ignore]`のまま将来(MPC等実装後)の完走確認に持ち越し。単スレッドNPS実測 約270〜300万(設計目標8-15Mには未達、MPC/安定石カット未実装のため許容)。
- **フェーズ1(エンジン核)完了**(2026-07-07)。T001〜T009すべてdone。次はフェーズ2(対局+盤UI、Workerプロトコル実装、PWA骨格)。
- フェーズ1完了条件(設計書§2.14): FFO endgame test #40-49 全問正解。
- T007・T008 のタスクファイルは T005/T006 完了後、実際のAPI形状を踏まえて作成する。
- 本プロジェクトはユーザー承認により「段階的フル実装」方針(設計書どおりRust/WASM+PVS/MPC、評価関数は軽量版→WTHOR学習は後続フェーズ)。
- T011・T012完了。重大な発見: `wasm32-unknown-unknown`では`std::time::Instant::now()`が実行時パニックするバグがT005/T008由来で存在しており(ネイティブの`cargo test`では検出不可)、T012のimplementerが実機確認中に発見し`web-time`クレートへの差し替えで修正済み(`engine/Cargo.toml`, `search.rs`, `protocol.rs`)。cargo test 44件・clippy・wasmビルドとも回帰なしを確認済み。
- 今後の運用改善: タスク実行中にスコープ外の緊急バグを発見した場合、影響が軽微(数箇所のみ・既存テストで検証可能)なら実装コミットに含めてよいが、その旨をコミットメッセージまたは作業ログに明示すること、と各implementerへの委譲プロンプトに明記する。
- reviewerより軽微な改善提案(次タスクで対応検討): `app/src/engine/worker.ts`に`engine.analyze()`が例外を投げた場合の`.catch()`フォールバックが無く、無応答のままハングしうる。
- T012の作業ログにあった「T011のothello.test.tsで白の初期合法手テストが失敗する」という記述は、verifierが再現確認したところ現在のHEADでは再現せず(並行編集中の一時的な観測と推測)。実害なし。
- T013完了。実機Playwright確認で複数パターンの対局が完走、パス発生も確認済み。reviewerの軽微な指摘: (1) CPU番中のuseEffect依存配列にlevelが含まれ、コメント「次の着手から反映」と実装(即座に再問い合わせ)が食い違う、(2) requestCpuMove失敗時にUIがフリーズ状態のまま復帰不能(console.errorのみ)。いずれも次タスクでの改善検討事項として申し送り(ブロッカーではない)。
- T014完了。オフライン動作(Service Worker + Cache Storage)まで実機確認済み。T010由来の`vite.config.ts`バグ(`vite preview`時にbaseが`/othello-trainer/`にならず本番相当の動作確認ができなかった)を発見・修正(`isPreview`判定を追加)。今後の軽微なフォローアップ: `sw.js`の`CACHE_VERSION`は`package.json`と手動同期が必要(自動連携なし)、`apple-touch-icon`はSVG指定でiOS非対応の可能性。
- **フェーズ2(GitHub Pagesで遊べる対局アプリ)完了**(2026-07-07)。T010〜T015すべてdone。GitHub Actionsでのデプロイが成功し、公開URL `https://giwarb.github.io/othello-trainer/` で実際にPlaywright経由の対局動作(着手・CPU応答)まで確認済み。ローカルmain(T001〜T015、計15コミット)をoriginへpush済み。
- 2026-07-08 ユーザーより追加依頼: CPU対局だけでなく、設計書通り定石練習・中盤練習・詰めオセロ・棋譜解析にモード分割し、言語化支援(`othello-trainer-design-verbalization.md`)も実装してほしい。どのモードでも「今打った手の評価」を即座に表示し、評価ソース(定石DB/中盤ヒューリスティック/終盤完全読み)を色分け、悪手判定閾値はユーザー調整可能(最善以外/差分n以上/順位n位以下)、悪手時は理由を分かりやすく表示。レスポンシブ必須。
- 実装順序はユーザー指定により設計書記載順(定石→中盤→詰め→棋譜解析)。定石DBはオセロクエストのデータを直接複製せず、公開情報(定石名鑑・戦術wiki等)から同じ定石体系を独自調査してDB化する方針(ユーザー承認済み、2026-07-08)。
- **T016で重大なデータ品質問題を検出**(verifier・reviewerが独立に発見): 30エントリ中約1/3(9〜10件)が、記載された出典URLに実際には存在しない座標列・名称だった(捏造/誤記載)。redoを指示済み(全エントリ実地再検証、誤出典の訂正、裏付け不能エントリの削除)。データ収集タスクでは「出典に記載があるはず」という記憶に頼らず、必ず実際にページを取得して文字列レベルで照合することを今後のタスク委譲プロンプトに明記する。
- T016最終合格(2回のやり直し後、2026-07-08)。原因はWebFetch(内部AI要約)の出力を事実確認せず信用したこと。2回目のやり直しでcurl+grepによる文字列照合に切り替え、35件全件検証。3回目の小修正(notes欄2箇所)を経て合格。教訓: データ収集系タスクの委譲では「WebFetchの要約ではなくcurl+grepで生データを直接照合すること」を明記する。
- `bookgen/joseki-research.json`: 35件の名前付き定石ライン(虎・兎・牛・ローズ系等)、出典URL付き。T017でDAG構造化・8対称正規化する。データには手の重み(頻度)情報が無いため、分岐点での重み付けはT017/T018で別途方針を決める必要がある(公開情報に頻度データが無ければ均等重み等で代用)。
- T017完了(定石DB DAG化: 35ライン→113ノード)。8対称正規化はverifier/reviewerが独立に数学的検証を実施し完全合格。
- T018完了(1回のredoを経て)。複数候補手一括評価API(`search_all_moves`、`allMoves:true`)を追加。境界条件(exact_from_empties+1)でのscore.type不整合バグと、time_ms未対応をreviewerが発見しやり直しで修正。各`MoveEval`に`is_exact`フラグ、レスポンスに`moves[].type`を追加し評価ソースを個別に判別可能にした。
- T019から先はフロントエンドUI実装フェーズ。評価バッジ(定石=青/中盤=黄/終盤=緑 のような色分け)と悪手判定設定(最善以外/差分n以上/順位n位以下)は全モード共通で再利用するため、定石練習モード本体より先に共通コンポーネントとして作る。
- T019完了。対局モードに評価バッジ・悪手検出を統合し実機確認済み(定石→中盤の切り替え、悪手理由表示、375px幅でのレスポンシブも確認)。reviewerの軽微な指摘(人間の着手ごとにrequestAnalyzeAllを呼ぶため、CPU応答がWorkerのFIFOキューで少し遅延しうる)は非ブロッキングとして記録、将来の改善候補。
- **T020で重大な設計不備を検出**(verifier・reviewerが独立に発見): 実データ(35ライン)では、短い2手ラインの終端と長いライン(18〜25手)の通過点が同じノードに同居するため、クリア判定(isLeaf即終了)が実プレイでは常に2手で発動してしまい、定石練習として機能していなかった。redoを指示済み(isLeafでもbookMoves非空なら継続、真の終端(bookMoves空)でのみクリア、通過した全ライン名をSRS記録)。これはT016/T017のデータ・DAG仕様自体は正しく、T020の統合設計(PracticeMode.tsx)がその特性を考慮していなかったことが原因。
- T020やり直し合格。実データで25手(コーパス最長ライン)セッションのクリア、22〜29件のライン名同時記録、本番WASMエンジンでの定石外判定(fetch差し替えなし)を実機確認。reviewerの非ブロッキング指摘: オプション機能`preferMovesTowardTarget`(相手の着手をtargetLineIdへ誘導)が、意図せず「毎回進行が変わる」というオセロクエスト式のランダム性を大きく損なっている可能性がある(target lineが辿れる限り相手の着手がほぼ決定的になる)。将来のポリッシュタスクで、誘導の強さを弱める、または数手ごとにのみ適用する等の調整を検討。
- リポジトリ直下に未追跡の`single_unlabeled.txt`(出所不明、T016のreviewerが以前から指摘)が残存。ブロッキングではないが今後のクリーンアップタスクで削除検討。
- T023完了(1回のやり直し後)。原因: `sw.js`の`CACHE_VERSION`固定値により本番キャッシュが永久固着し、ユーザーが「対局モードしか見えない」と報告。ビルド後処理でgitハッシュ+タイムスタンプを注入する方式で解決。やり直し理由: 初回実装のプレースホルダー全置換がsw.js内の説明コメントまで巻き込み本番配信物を破壊(reviewerが実際に本番sw.jsをcurlで確認して発見)。代入行限定の正規表現に修正して解消。
- T026完了。定石練習のクリア画面で「本当にセッションを終わらせた最終ノードの定石名」を主役表示、それ以外の通過ラインを折りたたみ補足表示に分離。reviewerの非ブロッキング指摘: `lookup`が`null`の防御的ケース(通常発生しない)で、途中経由リストに主役表示と同じ名前が重複表示される軽微な不整合あり。将来のポリッシュタスクで対応検討。
- T025完了。ユーザー指摘(定石が少なすぎる、虎系のローズビル・酉が無い)を受け、`bookgen/joseki-research.json`を35→90ラインに拡充(bassy84.netのサイトマップから機械的にURL抽出、curl+grepで着手列をverbatim検証、T016の教訓を厳守)。「花形（ローズビル）基本形」「酉定石」(3ページの共通接頭辞から復元、経緯をnotesに正直に開示)を収録。DAGは507ノードに再構築。verifierが13ラインを独自にcurl+grep再検証し全件一致、90ライン全件の座標記法・合法性も機械検証済み。データ捏造等の問題は検出されず。
- T021完了(1回のやり直し後)。やり直し理由: reviewerが独立コードレビューで2件の重大バグを発見(verifierは受け入れ基準コマンド中心で検出できず): (1) `checkEnd`が「片側だけ合法手なし(パスすべきだが終局でない)」を誤って終局判定し、クリア到達を自動化で一度も再現できなかった真因だった可能性が高い、(2) `app/src/midgame/pool.ts`と`app/src/joseki/db.ts`のIndexedDBバージョン不整合により、中盤練習モード使用後に定石練習(T020)のSRS機能が`VersionError`で静かに壊れる回帰バグ(実際に再現確認された)。修正: `resolveMover.ts`(`gameLoop.ts`の`afterMove`と同じ規則を切り出し)、`db/appDb.ts`(DB名・バージョン・全ストアを一元管理する共通モジュール)を新設し両モジュールに適用。やり直し後、verifier・reviewerとも合格。教訓: **複数モジュールが同じIndexedDBを扱う場合、バージョン管理は必ず一元化すること**。今後同種のタスク(IndexedDBを使う新機能)の委譲時は、既存のDBオープンロジックを共有するよう明記する。「クリア」到達の実機確認は自動化では最後まで再現できず、ユーザーによる手動確認を推奨事項として引き継ぎ。
- 2026-07-09 ユーザー指示: 「設計書にある機能が完成するまで、ずっと続けてください。途中でやめないで。」CLAUDE.mdに「自律継続ルール」を追加(ユーザー確認を待たず、tasks/STATUS.mdの全ロードマップ項目がdoneになるまでタスク分解・委譲・検証・次タスク着手を繰り返す。停止してよいのは全完了/ユーザー明示指示/真にユーザー判断が必須な分岐のみ)。以後、フェーズ4-C(詰めオセロ)以降も継続してタスク化・実装していく。
- 2026-07-08 ユーザーより運用ルール追加(CLAUDE.mdのプロジェクト固有情報にも反映済み): (1) 今後すべてのタスクで、完了時にmainへpush・GitHub Actionsデプロイ確認・`playwright`での本番Pages URL上での実機確認まで受け入れ基準に含めること。(2) 評価関数(eval)に関わる検証では、既知の強豪オセロAI実装(Edax等)との比較で評価値の妥当性を確認すること。後者への対応としてT022(Edax比較基盤構築)を新規起票。
- T021(中盤練習モード)着手前。設計書§4のうちWTHOR由来局面・実戦模倣相手強さはデータ未導入のためスコープ外とし、代替として自己対局によるランダム中盤局面生成を採用する方針でタスク化した。出題プールのIndexedDBストアは棋譜解析モード(未実装)からの将来連携を見据え汎用的な`source`フィールド付きで設計するよう指示済み。
