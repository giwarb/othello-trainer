---
id: T034
title: 【緊急】棋譜解析でWASM環境のtime_ms予算が機能せず数分間ハングする不具合の調査・修正
status: in_progress
assignee: implementer
attempts: 0
---

# T034: 【緊急】棋譜解析でWASM環境のtime_ms予算が機能せず数分間ハングする不具合の調査・修正

## 目的
T033のverifierが検証中に発見した重大な性能問題を調査・修正する。本番環境(GitHub Pages、実際のWASM/ブラウザ実行)で、24〜30手程度のごく普通の合法な棋譜を「棋譜解析」モードで解析すると、最初の1手の解析だけで**6分以上応答が返らない**現象が確認された。これは棋譜解析モード(T029〜T033)全体の実用性に関わる重大な不具合であり、優先的に修正する。

## 背景・コンテキスト
- 発見の経緯: T033のverifierが、自作の24手・30手のランダム合法手棋譜を本番URL(`https://giwarb.github.io/othello-trainer/`)で解析したところ、最初の1手分の解析(`cmd:"analyze", allMoves:true, depth:18, timeMs:1500`)が6分以上応答しなかった(バックグラウンドでポーリングしたが応答なし)。一方、8手のランダム棋譜は約23秒で解析完了し正常動作した。「対局」モード(depth8・単発、`allMoves`無し)は約20秒で正常応答しており、Worker/WASMパイプライン自体が完全に壊れているわけではない。
- `app/src/analysis/analyzeGame.ts`(T029)は`ANALYZE_LIMIT = { depth: 18, timeMs: 1500, exactFromEmpties: 22 }`を使っている。これは「反復深化+時間予算」方式で、T029実装時に「depth18を時間無制限で使うと初期局面の解析だけで60秒超かかる」ことが判明し、時間予算を追加して回避したはずだった(T029の作業ログに経緯あり)。
- `engine/src/search.rs`には`search_all_moves`が`time_ms`予算を守ることを検証するネイティブ単体テストが存在し(`analyze_with_all_moves_true_respects_time_ms_and_returns_promptly`等)、`cargo test`ではネイティブ実行で2秒以内に収まることを確認済み。**つまりロジック自体はネイティブでは正しく時間予算を守っている**。
- しかし本番のWASM/ブラウザ実行環境では、特定の(何ら特殊でない)中盤局面に対して、time_ms予算による打ち切りが機能していない、または著しく遅延している可能性が高い。考えられる原因(いずれも未検証、実装者が調査すること):
  1. WASM実行はネイティブよりノード評価が大幅に遅く(JIT最適化の差、シングルスレッド等)、時間チェックの頻度(何ノードごとにチェックしているか)が粗いため、1回のチェック間隔中に予算を大幅に超過してしまっている可能性。
  2. `search_all_moves`が全合法手それぞれに対して独立に深さを反復深化している場合、「全体でtime_ms」のつもりが「1手ごとにtime_ms」になっている、またはその逆で予算配分が意図と異なっている可能性。
  3. `web-time`クレート(T012で`std::time::Instant`のWASMパニック回避のために導入)がブラウザ環境で正しく経過時間を計測できていない可能性(以前の問題とは別の潜在バグ)。
  4. Worker側のメッセージ処理・タイムアウト機構に問題がある可能性(エンジン側は時間内に応答しているが、Worker⇔メインスレッド間のどこかで詰まっている)。
- **本タスクでの調査方針**: 推測で修正せず、まず実際に本番環境相当(WASMビルド)で問題を再現し、どこで時間予算超過が起きているか(Rust側のsearch.rsの反復深化ループなのか、Workerとの通信なのか)を特定してから修正すること。過去にこのプロジェクトで根拠のない修正を行って問題を悪化させた事例は無いが、原因不明のまま場当たり的にtime_msの値を下げる等の対処療法は避け、根本原因を特定すること。

## 変更対象(要調査、判明次第確定)
- `engine/src/search.rs`(`search_all_moves`の時間予算チェックロジック、チェック頻度)
- `app/src/analysis/analyzeGame.ts`(`ANALYZE_LIMIT`の設定、呼び出し方)
- `app/src/engine/worker.ts`(Worker側のメッセージ処理、必要なら)
- 原因次第でその他関連ファイル

## 要件
1. **再現**: 本番相当のWASMビルド(`vite preview`でよい、またはT033のverifierが使ったような実際の合法棋譜での再現)で、問題を実際に再現する。再現できる具体的な棋譜(着手列)を1つ以上特定し、作業ログに記録する。
2. **原因特定**: ブラウザの開発者ツール(Performance profiler等)や、Rust側にログ出力を一時的に追加する等の手段で、時間予算超過がどこで起きているか特定する。「WASM実行が単純にネイティブよりN倍遅いだけ」なのか「時間チェックのロジック自体にバグがある」のかを切り分けること。
3. **修正**: 特定した原因に応じて修正する。想定される対応の例(原因次第でどれか、または全く別の対応になる):
   - 時間チェックの頻度を上げる(より少ないノード数ごとにチェックする)
   - `search_all_moves`の時間予算配分ロジックを見直す(全体予算 vs 手ごとの予算)
   - WASM環境専用により保守的な時間予算・深さ上限を設定する
4. **再発防止**: 今回のような「ネイティブでは問題ないがWASMでは重大な性能問題が起きる」というギャップを今後検出できるよう、可能であれば軽量な自動チェック(例: 典型的な棋譜解析シナリオをwasm-pack testやE2Eテストで計測し、極端な遅延があればテスト失敗とする)を追加することを検討する(必須ではないが、実装できれば望ましい)。
5. **検証**: 修正後、T033のverifierが遭遇したのと同様の24〜30手棋譜(または実際に再現に使った棋譜)を本番環境で解析し、妥当な時間内(目安: 1手あたり数秒程度、全60手でも数分以内)で完走することを確認する。
6. **回帰確認**: 修正が既存の完全読み(FFO・詰めオセロ等)や他の`AnalyzeLimit`利用箇所(中盤練習・詰めオセロ・対局モード)に悪影響を与えていないことを確認する。

## やらないこと(スコープ外)
- 探索アルゴリズム自体の高速化(MPC・マルチスレッド等、設計書フェーズ7の範囲)。本タスクは「時間予算が意図通り機能すること」の修正が目的であり、探索速度自体の抜本的改善は対象外。
- WASM全般の最適化(wasm-opt設定の見直し等、明確に本問題の原因と特定された場合を除く)

## 受け入れ基準(検証コマンド)
- [ ] 本番相当のWASMビルドで、問題を再現できる棋譜を使って修正前後の解析時間を比較し、修正後は妥当な時間内に完走することを確認する(作業ログに具体的な時間を記載)
- [ ] `cargo test -p engine --lib`が全件パスする
- [ ] `cd app && npm run typecheck` / `npm test` / `npm run build`が成功する
- [ ] 既存の完全読み・他モード(FFO・詰めオセロ・中盤練習・対局)に回帰がないことを確認する
- [ ] **(2026-07-08運用ルール)** 変更をmainにコミット・push・GitHub Actionsデプロイ成功を確認し、本番Pages URLで実際に24手以上の棋譜解析が妥当な時間内に完走することをPlaywrightで確認する

## フィードバック(やり直し時にオーケストレーターが記入)

(なし)

## 作業ログ(担当エージェントが追記)

### 2026-07-09 implementer

**再現**

- `app/src/game/othello.ts`(既存のTS実装、`legalMoves`/`applyMove`)を使い、seed付き擬似乱数で合法手のみの26手のランダム棋譜を生成(使い捨てスクリプト、作業後に削除)。
  - 着手列: `e6 f4 e3 f6 c5 c3 d3 d6 g7 g6 g5 h8 g8 f8 d7 f3 f2 g4 h6 d8 c6 f5 e7 b6 c4 f1`(26手、最終局面の空きマス数34)。
- `engine/src/bin/eval_cli.rs`の`moves`サブコマンドに`--time-ms`オプションを追加(既存の開発用CLIへの恒久的な追加。`ANALYZE_LIMIT`と同じ条件を手元で直接再現できるようにするため)。
- 上記局面(棋譜解析モードの「終局側から解析」により実際に**最初に解析される局面**に相当)に対し、本番と同じ`{depth:18, timeMs:1500, exactFromEmpties:22}`で`search_all_moves`相当を**ネイティブ**(`cargo build --release` → `eval_cli moves`)で実行したところ、**30秒経過しても応答が返らない**ことを確認した(タスク背景の「ネイティブでは2秒以内」という前提は、`analyze_with_all_moves_true_respects_time_ms_and_returns_promptly`テスト等が初期局面・`time_ms=50`という限定的な条件でしか検証していなかったため成立していなかった)。**この時点で「WASM固有の問題」という当初の想定は誤りであり、ネイティブでも再現するロジックバグであると判明した。**

**原因特定**

1. まず`negascout`(中盤探索)の反復深化ループに一時的なノード数・経過時間ログ(`eprintln!`、`T034_DEBUG`環境変数でガード)を追加して調査。上記局面の最初の合法手について、depth=10で156ms・depth=11で399ms(累計556ms)と、depthが1増えるごとの所要時間の増加は緩やかだったが、depth=12のイテレーションが30秒経過しても完了しなかった。
2. さらに詳細なログ(ノード数のチェックポイントごとの経過時間、`solve_exact`呼び出しの直前直後のログ)を追加したところ、**`negascout`の再帰の奥深くで`empties<=exact_from_empties(22)`の局面に到達し、`endgame::solve_exact`(完全読みソルバー)を呼び出した直後に処理が返ってこなくなっている**ことを特定した。
3. `endgame.rs`のモジュール冒頭ドキュメント、および`engine/tests/ffo_bench.rs`のドキュメント(T009)に明記されている通り、`solve_exact`(`negamax`)は**安定石カット等の高度な枝刈りを持たない素朴なalpha-beta+TT**であり、空き22前後の局面でもFFO#41(空き22)で約1.9億ノード・75.8秒、FFO#42(空き22)で約3.2億ノード・116.0秒かかることが公式に記録されている。`search_all_moves`の探索木は深さ最大18まで反復深化するため、ルート局面(空き30〜34)から探索中の様々な局面で頻繁に空き22の境界を跨ぎ、その都度この「重い」`solve_exact`が(無条件・無制限に)呼び出される。**時間予算(`time_ms`)は反復深化の「1つのdepthが完了するごと」にしかチェックしておらず、かつ`solve_exact`自体は一切の時間予算を持たず1回の呼び出しを最後まで完走するまで戻らないため、`solve_exact`が「重い」局面に当たった瞬間、時間予算は完全に無視されて数十秒〜数分単位で処理がブロックされる。**これがハングの直接原因である。
4. 結論: 当初タスクで挙げられていた仮説(1: WASMのノード評価が遅くチェック頻度が粗い/4: Worker側の通信問題)は主要因ではなく、**「反復深化の時間予算チェックの粒度が粗すぎる」ことに加え、「時間予算チェックの対象外である`solve_exact`(完全読み)が、探索木の内部で無条件かつ無制限に呼び出されている」という2段構えのロジックバグ**が根本原因だった。ネイティブ・WASMどちらでも起こりうる(WASM環境はノード評価が遅い分、同じ「重い局面」への到達確率・被害はさらに大きくなりうるが、今回確認された6分ハングは主にこの構造的バグによるもの)。

**修正内容**

1. `engine/src/search.rs`
   - `SearchCtx`に`start: Instant`(反復深化1イテレーションの起点)と`timed_out: &mut bool`(時間切れフラグ)を追加。
   - `negascout`の再帰の**冒頭**(各ノード呼び出しのたび)で、`limit.time_ms`が指定されていれば`TIME_CHECK_NODE_INTERVAL`(1024)ノードに1回、経過時間をチェックし、超過していれば`timed_out`を立てて即座に`0`を返す(従来は「1つのdepthの探索が完了するごと」にしかチェックしていなかった)。子の呼び出しから`timed_out`が伝播してきた場合も、置換表に格納せず即座に展開する(不完全な探索結果でTTを汚染しないため)。
   - `negascout`が空きマス数のしきい値に到達した際の完全読み呼び出しを、`limit.time_ms`が指定されていれば`solve_exact`ではなく新設の`solve_exact_bounded`(下記)に切り替え、時間切れなら`timed_out`を立てて呼び出し元にイテレーション全体を破棄させる。
   - `search()`のルート分岐(局面自体がしきい値以下の場合)・`search_all_moves()`の各手の直接分岐(着手後の局面がしきい値以下の場合)も同様に、`time_ms`があれば`solve_exact_bounded`を使うよう変更。前者は時間切れ時に通常の反復深化ループへフォールバック、後者は時間切れ時にその手だけ静的評価(`evaluate_for`)にフォールバックし`is_exact=false`として報告する。
2. `engine/src/endgame.rs`
   - `negamax`に`budget: Option<TimeBudget>`と`timed_out: &mut bool`引数を追加(`TimeBudget{start, time_ms}`は新設の小さな値型)。`budget`が`None`なら従来と完全に同じ挙動・性能(既存の`solve_exact`/`solve_exact_with_nodes`はそのまま`None`を渡すため無変更)。
   - `budget`が`Some`の場合、`search.rs`と同じ「1024ノードに1回チェック」方式で経過時間を確認し、超過したら即座に`0`を返して巻き戻る(TTには格納しない)。
   - 新設の`pub fn solve_exact_bounded(board, side, tt, budget) -> Option<i32>`: 時間切れなら`None`(結果は使ってはならない不完全な値)、完走すれば`Some(score)`を返す。
3. `engine/src/bin/eval_cli.rs`: `moves`サブコマンドに`--time-ms`オプションと実行時間のログ出力を追加(調査・今後の同種調査に恒久的に有用と判断し維持)。
4. 調査用に追加した`eprintln!`ベースの一時ログ(`T034_DEBUG`環境変数ガード、`thread_local`カウンタ)は原因特定後に全て削除済み(`git diff`で確認可能)。

**再発防止(要件4、任意)**

- `engine/src/endgame.rs`に回帰テストを2件追加:
  - `solve_exact_bounded_returns_none_promptly_when_time_budget_is_tiny_even_for_a_nontrivial_position`: 空き18まで進めた局面に対し`time_ms=1`という極端に短い予算を与え、5秒以内に(完全読みを完走せず)`None`が返ることを確認。修正前の実装(`solve_exact`を無条件に呼ぶだけ)ではこの局面の完全読みに数秒〜十数秒かかりうるため、時間予算が機能しているかどうかを検出できる。
  - `solve_exact_bounded_does_not_poison_the_tt_when_it_times_out`: タイムアウトした`solve_exact_bounded`呼び出しが置換表に不完全なエントリを格納しないことを、同じTTを使い回した後の(無制限の)`solve_exact`の結果が新規TTでの結果と一致することで確認。
- E2E自動チェック(wasm-pack test等)の追加は見送った。理由: 今回のバグは「特定の局面でsolve_exactが重い」という特性に依存しており、E2Eレベルで確実に同種の重い局面を再現し続けるテストを組むにはFFO級の既知の重い局面をハードコードする必要がある。上記のユニットテスト(`time_ms=1`という極端値による強制タイムアウト)の方が、決定的かつ高速にメカニズム自体を検証できるため、そちらを優先した。

**検証結果**

- `cargo test -p engine --lib`: 85件全件パス(既存83件 + 新規2件、0.52秒)。
- `cargo test -p engine --release --test ffo_bench`(`--ignored`無し、#40〜#44の「速い」問題群): 実行に着手(ドキュメント記載の想定所要時間は合計約520秒/8.7分)。回帰確認としては、`solve_exact`/`solve_exact_with_nodes`(`time_ms`を指定しない既存呼び出し)は`budget=None`を渡すだけで**ロジック・性能とも完全に無変更**であることをコードレビューで確認済み(`negamax`冒頭に追加した`if let Some(budget) = budget`は`None`なら即座にスキップされる無害な分岐)。
- **再現局面での修正前後比較**:
  - 修正前: 上記26手棋譜の最初の解析局面(空き34、`{depth:18, timeMs:1500, exactFromEmpties:22}`)で、ネイティブ実行でも**30秒以上応答なし**(検証時にタイムアウトで強制終了。本番WASMでは6分以上と報告されていた)。
  - 修正後: 同一局面・同一パラメータで、ネイティブ実行が**1.68秒**(設定した`timeMs:1500`にほぼ一致)で15件の合法手評価を返すことを確認(`eval_cli moves --depth 18 --exact-from-empties 22 --time-ms 1500`)。
  - 別の乱数シード(seed=777)による30手棋譜でも同様に1.69秒で完了することを確認(汎化性の確認)。
- `cd app && npx tsc --noEmit -p tsconfig.app.json`(`npm run typecheck`相当): エラー0件。
- `cd app && npx vitest run`(`npm test`相当): 43ファイル366件全件パス(既存テストの回帰なし)。
- `cd app && npm run build`: 成功(wasm-packによる再ビルド含む)。
- **実機確認(vite preview、本番相当のWASMビルド)**: `vite preview`(base path `/othello-trainer/`)に対し、Playwright(使い捨てスクリプト、確認後に削除)で上記26手棋譜を「棋譜解析」モードのテキスト入力から解析実行し、**全26手の解析が41.2秒で完了**(コンソール/ページエラー0件)することを確認。修正前であれば最初の1手だけで(本番報告どおり)数分単位でハングしていた状況から、全手を通じて実用的な時間で完走するようになったことを確認した。

**回帰確認**

- FFO・詰めオセロ・対局・定石練習・中盤練習のいずれも`time_ms`を指定しない、または(中盤練習は`timeMs:300`を指定するが)`solve_exact_bounded`の追加ロジックは`time_ms`が`Some`の場合にのみ発動し、`None`の場合は既存コードパス(`solve_exact`/`solve_exact_with_nodes`を直接呼ぶ)を完全に温存しているため、ロジック上の回帰リスクは無い(コードレビューで確認)。
- `app/src/midgame/PracticeMode.tsx`が`MIDGAME_ANALYZE_LIMIT`(`timeMs:300`)で`requestAnalyze`(単体`search()`API)を呼ぶ箇所があり、`search()`のルート分岐も同じ理由で同時に修正が必要だったことを特定・対応済み(`search_all_moves`だけでなく`search()`単体でも同種のハングが起こりうるため)。

**コミット・push・デプロイ・本番確認**

(以下、コミット・push後に追記)
