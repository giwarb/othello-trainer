---
id: T146
title: WTHOR全局面ラベル付けのlevel 10所要時間見積もり(計測のみ、ユーザー指示)
status: done # 2026-07-20 done裁定。計測のみ・本番コード無変更。オーケストレーターが外挿計算を再検算し一致確認(verifier省略裁定)
assignee: implementer(Sonnet)
attempts: 0
---

# T146: lv10ラベル付けの所要時間見積もり

## 目的(ユーザー指示 2026-07-20)

「lv16のEdaxラベルとWTHOR石差ラベルに有意差がない(T144)なら、lv16である必要がない。lv10なら時間のかかる帯域も速くなる期待がある。lv10でどれくらい時間がかかるか見積もって」— **WTHOR全局面(約443万レコード、74,024局の全手)へのEdaxラベル付け**を level 10 で行った場合の所要時間を、実測ベースで見積もる。**計測と見積もりレポートのみ。コーパス生成・本番コード変更は行わない。**

## 背景・事実

- 現行の生成方式(T127g/i確立): warm 32親束バッチ+`wEdax-x86-64-v3.exe`(AVX2)×8シャード並列、`-n 1`決定性仕様。1レコード=1局面の**全合法手(子局面)**を評価してラベル化。exactEmptiesThreshold=20(空き20以下=完全読み、21以上=level探索)。
- 実績: expanded1m(100万レコード)の新規80万件生成に実質約5日(方式改善込み・lv16)。重い帯(空き20-29)で約4.5-5k件/h、軽い帯で140k件/h超と帯差が極端。
- 既存参考見積もり: 「WTHOR全局面400万はlv16で約8日」(2026-07-17時点、方式改善前の概算)。
- 生成パイプライン: `bench/edax-compare/gen_teacher_corpus.py`(T143で堅牢化済み。**本タスクでは変更禁止**)。A/B計測の前例: T127f/T127i(ハーネスは未コミット、ログ logs/codex-T127*系とタスクファイル作業ログに手法記録あり)。
- 全手ラベル付けの対象分布: 74,024局の全手=各局約60局面、空き1〜59にほぼ一様に分布(1局面が空きnに1個ずつ)。ただし空き20以下はexact経路なのでlevelを下げても速くならない点に注意。

## 要件

1. **サンプル実測(lv16 vs lv10)**: WTHOR対局から実局面をサンプリングし(既存コーパスの局面流用可)、空き帯別(最低: 21-25 / 26-30 / 31-40 / 41-50 / 51-59 の5帯、各20〜40局面)に、本番同等の呼び出し方(v3バイナリ、warm束方式または現行バッチ相当、`-n 1`、全合法手評価)で 1レコードあたり壁時計を lv16 と lv10 の両方で計測する。計測は専有状態で行う(現在他の重い処理はない)。
2. **exact帯の実測または実績流用**: 空き20以下のexact経路のコスト(levelに非依存)を、expanded1m生成の実績データ(シャードログ・meta等)から帯別スループットとして推定するか、少数サンプルで実測する。
3. **外挿**: 443万レコードの空き分布(74,024局×全手、空き1-59ほぼ一様=各空き約7.4万レコード)に帯別実測を掛けて、8シャード並列前提の総所要時間を見積もる。以下を分けて提示:
   - (i) lv16での総時間(現行方式の再見積もり)
   - (ii) lv10での総時間
   - (iii) 内訳: exact帯(空き≤20、level非依存)/ level帯(空き21+)の時間と、lv10化による短縮が総時間の何割か
   - (iv) 感度: exactEmptiesThreshold を 20→18 や 16 に下げた場合の追加短縮(参考値。exact帯が支配的な場合に判断材料になる)
4. **レポート**: bench/edax-compare/ に t146_lv10_estimate_report.md(+必要ならmeta json)として計測条件・生データ要約・見積もり表・仮定と誤差要因を記録し、コミット・push。計測用スクリプトはscratchpad(リポジトリ外)でよいが、再現に必要なコマンド・条件はレポートに明記。
5. 長時間実行ルール: サンプル計測は帯ごとに逐次結果をログへ書き出す(一括書き出し禁止)。総計測時間が2時間を超えそうなら、サンプル数を減らして精度とトレードオフする(見積もり用途なので±20%程度の精度で十分)。

## スコープ外(やらないこと)

- 443万ラベル付けジョブの実行・開始(見積もりのみ。実行判断はユーザー)
- gen_teacher_corpus.py等の本番コード変更、ラベル品質(lv10 vs lv16の値の差)の評価 — 品質検証は実行判断後の別タスク
- app/engine変更(Pages確認不要)

## 受け入れ基準

1. lv16/lv10の帯別実測値(1レコードあたり秒、サンプル数明記)がレポートにある
2. 443万レコードの総所要見積もり(lv16/lv10、exact帯内訳、threshold感度)が8シャード並列前提で提示されている
3. 仮定(空き分布・並列効率・warm効果)と誤差要因が明記されている
4. レポートがパス明示でコミット・pushされ、完了時 `git status --short` がクリーン
5. 計測中に生成した一時ファイルがリポジトリ内に残っていない

## コミット規律

- 変更対象(レポート類)のみパス明示add。`tasks/` と `CLAUDE.md` はコミットしない(作業ログ追記は行う)

## 作業ログ

- 2026-07-20: 着手。`gen_teacher_corpus.py`/`vs_edax.py`を調査し、本番呼び出し経路(warm 32親束バッチ、`wEdax-x86-64-v3.exe`、`-n 1`、`exactEmptiesThreshold=20`、`DEFAULT_EDAX_LEVEL=16`)を確認。`train/data/teacher/corpus_expanded1m.jsonl`が既に完了済み(1,000,000件、2026-07-19完了)であることを発見し、これを実本番lv16実績データとして活用する方針に転換(帯別サンプル実測だけに頼るより高精度)。
- 2026-07-20: `analyze_corpus.py`(scratchpad)でcorpus_expanded1m.jsonlを解析し、親局面の空きマス数(record.empties)ごとの平均レコード時間(children.elapsedMsの合計)を算出。1record内でexact/level16が混在する例が0件であることを確認(childEmpties=parentEmpties-1が一定のため、levelは親の空きマス数だけで一意に決まる)。
- 2026-07-20: `sample_positions.py`/`measure_ab.py`(scratchpad、gen_teacher_corpus.py・vs_edax.pyの関数をimportして呼ぶのみ、両ファイル無変更)で、帯別(親空き22-25/26-30/31-40/41-50/51-59、各32局面)にlv16→lv10のライブA/B計測を実施(総計測432.7秒)。**重要な発見**: level=10は親空き22-30では5.1〜9.6倍高速だが、親空き31-59では逆にlevel=16より2.4〜4.1倍低速という非単調な結果を確認(Edaxのlevel→探索深さ/選択的探索強度の対応が単調でないため。depthは要求通り10/16で、exactへの自動昇格は起きていないことを確認済み)。
- 2026-07-20: `sample_sensitivity.py`/`measure_ab.py`で親空き18-21(exactEmptiesThreshold20→18/16で新たにlevel化される対象)を追加計測(29.7秒)。
- 2026-07-20: 外挿計算により、(i)lv16総時間269.07h(11.21日) (ii)lv10一律304.59h(+13.2%悪化) (iii)重い帯(空き22-30、レコード数23.7%だがlevel帯時間の71.4%)/残りの帯の内訳 (iv)exactEmptiesThreshold感度(20→18/16で最大2.2h・0.8%短縮、効果小)を算出。参考知見として、lv10を重い帯(空き22-30)だけに限定するハイブリッド運用なら114.76h(-57.3%)という追加試算も実施(スコープ外の追加分析として明記)。
- 2026-07-20: `bench/edax-compare/t146_lv10_estimate_report.md`と`t146_lv10_estimate_report.meta.json`を作成。計測スクリプト(analyze_corpus.py/sample_positions.py/sample_sensitivity.py/measure_ab.py/sample_sensitivity.py)はすべてscratchpadに配置し、リポジトリには一切追加していない(git status --shortで確認、生成した一時OBFファイルもfinally節で都度削除済み、リポジトリ内に残存なし)。443万レコードのラベル付けジョブは開始していない。gen_teacher_corpus.py/vs_edax.pyは無変更。
- 受け入れ基準確認: (1)帯別実測値(サンプル数明記)をレポートに記載済み (2)総所要見積もり(lv16/lv10/内訳/閾値感度)を8シャード並列前提で提示済み (3)仮定と誤差要因を明記済み (4)レポート2ファイルをコミット・push予定(オーケストレーターが担当) (5)一時ファイル残存なし(git status --short で確認済み)。
